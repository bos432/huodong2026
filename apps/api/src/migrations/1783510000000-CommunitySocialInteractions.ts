import{MigrationInterface,QueryRunner}from"typeorm";
export class CommunitySocialInteractions1783510000000 implements MigrationInterface{name="CommunitySocialInteractions1783510000000";async up(q:QueryRunner):Promise<void>{await q.query("ALTER TABLE `community_posts` ADD `favoriteCount` int NOT NULL DEFAULT 0, ADD `deletedAt` datetime NULL");await q.query("ALTER TABLE `community_post_comments` ADD `parentId` int NULL, ADD `mentionUserIds` json NULL, ADD `deletedAt` datetime NULL");await q.query("CREATE TABLE `community_post_favorites` (`id` int NOT NULL AUTO_INCREMENT, `postId` int NOT NULL, `userId` int NOT NULL, `createdAt` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6), UNIQUE INDEX `IDX_community_post_favorite_user` (`postId`,`userId`), PRIMARY KEY (`id`)) ENGINE=InnoDB");await q.query("CREATE TABLE `community_user_follows` (`id` int NOT NULL AUTO_INCREMENT, `followerUserId` int NOT NULL, `followedUserId` int NOT NULL, `createdAt` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6), UNIQUE INDEX `IDX_community_user_follow_pair` (`followerUserId`,`followedUserId`), PRIMARY KEY (`id`)) ENGINE=InnoDB");await q.query("CREATE TABLE `community_notifications` (`id` int NOT NULL AUTO_INCREMENT, `userId` int NOT NULL, `type` varchar(24) NOT NULL, `postId` int NULL, `commentId` int NULL, `actorUserId` int NULL, `title` varchar(160) NOT NULL, `content` varchar(500) NOT NULL, `readAt` datetime NULL, `createdAt` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6), INDEX `IDX_community_notification_user_read` (`userId`,`readAt`), PRIMARY KEY (`id`)) ENGINE=InnoDB");}async down(q:QueryRunner):Promise<void>{await q.query("DROP TABLE `community_notifications`");await q.query("DROP TABLE `community_user_follows`");await q.query("DROP TABLE `community_post_favorites`");await q.query("ALTER TABLE `community_post_comments` DROP COLUMN `deletedAt`, DROP COLUMN `mentionUserIds`, DROP COLUMN `parentId`");await q.query("ALTER TABLE `community_posts` DROP COLUMN `deletedAt`, DROP COLUMN `favoriteCount`");}}
