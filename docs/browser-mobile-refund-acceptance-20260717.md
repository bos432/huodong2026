# 移动管理端退款工作台浏览器验收

验收时间：2026-07-17

## 环境与账号

- H5：`http://127.0.0.1:5173/#/pages/admin/refunds`
- API：`http://127.0.0.1:3000/api`
- 账号：`showcase_finance`
- 租户：慢π演示中心
- 视口：默认桌面宽度及 390 x 844 手机视口

## 验收结果

| 项目 | 结果 |
|---|---|
| 登录与权限 | 登录成功，移动 bootstrap 返回财务查看及退款处理能力，底部导航显示退款入口。 |
| 列表 | 待审核状态真实加载 8 条保留退款，显示退款号、活动、金额、订单、支付方式、时间和原因。 |
| 筛选 | 全部、待审核、提交中、处理中、失败、已通过、已完成、已拒绝 8 类状态可切换；失败状态当前为空并正确展示空态。 |
| 审核保护 | 通过/拒绝按钮仅退款管理权限可见；打开通过确认弹窗后搜索和其他写操作锁定，取消后恢复，未提交业务变更。 |
| 异常处置 | 页面提供渠道回执扫描；失败退款提供“核对后重试”，要求填写渠道核对说明，并复核账号及租户上下文。当前库无失败退款，因此重试运行态保留给后续受控失败数据验收。 |
| 响应式 | 390 x 844 下页面宽度 390、文档滚动宽度 390，无横向溢出；状态筛选使用横向滚动，不挤压卡片。 |
| 控制台 | 页面 console warning/error 为 0。 |
| 数据保护 | 仅打开并取消审核弹窗，未确认审核、拒绝、重试或回执扫描，退款和资金数据保持不变。 |

## 自动化证据

- `menu-integrity.spec.ts`：11 项通过，包含移动退款路由、导航权限、审核、失败重试、回执扫描和账号/租户切换保护契约。
- H5 生产构建通过。
- 微信小程序生产构建通过，授权配置补丁正常写入。
- `git diff --check` 无空白错误，仅 Windows 行尾提示。

## 待外部验收

- 正式微信/支付宝失败退款的真实渠道重试和回执状态流转。
- 微信开发者工具及真机上的弹窗输入、网络切换和安全区适配。

