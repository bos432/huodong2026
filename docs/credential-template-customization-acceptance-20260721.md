# 证书模板完整自定义功能融合验收

验收时间：2026-07-21  
本地环境：`http://127.0.0.1:18080`

## 交付范围

- 统一支持公益贡献凭证、公益大使证书、志愿服务证书、城市共建证书、课程结业证书五类模板。
- 支持平台模板、商家模板、系统默认三级回退；商家数据按租户隔离。
- 支持草稿保存、SVG 成品预览、发布、版本历史、恢复历史版本为草稿。
- 支持标题、英文标题、说明、详情标签、发证单位、声明、五类颜色、Logo 文字、Logo 图片、背景图、印章、签名、编号前缀和公开姓名策略。
- 模板素材仅允许 HTTPS、`/uploads/` 和受控 data image，拒绝 JavaScript 与 HTTP URL。
- 新发证书固化模板版本与配置快照；历史证书首次查看时固化，后续模板发布不改变旧证书。
- 课程原有课程级证书模板继续保留，并覆盖统一课程模板的名称、说明、发证单位和背景。
- 公益贡献凭证沿用稳定 `MPCG` 编号，避免历史验真链接失效；本人和后台鉴权预览展示完整姓名，公开显示服从模板的脱敏/完整/隐藏策略。

## 数据库与权限

- 已执行 `CredentialTemplateCustomization1783990000000`：新增模板和版本表，并给证书、公益交易增加快照字段。
- 已执行 `BackfillCredentialTemplatePermissions1784000000000`：仅给原本具备公益或课程管理能力的显式权限账号补模板查看/管理权限。
- 原数据库、公益流水、证书、上传文件、测试账号和历史候选包均保留。
- 未登录模板 API 返回 401；不具备新权限的账号由统一权限守卫返回 403。

## 自动化验证

- 模板与 SVG 专项：6 文件、104 项通过。
- API 全量：175 文件、997 项通过（权限回填迁移加入后再次执行最终全量回归）。
- API、后台、H5、微信小程序生产构建通过。
- 密钥扫描：1,353 个源码文件通过。
- 运行监控：`status=ok`、`alerts=0`。
- API、MySQL healthy，Nginx running；差异检查无空白错误。

## 真实接口融合流程

- 平台管理员读取 5 类模板成功；预览 SVG 包含自定义标题。
- 保存城市共建草稿后线上模板未变化；发布 v1 后生效；恢复历史仅修改草稿；发布 v2 后恢复默认视觉。
- 租户管理员 `showcase_admin` 作用域为 `tenant:23`；草稿隔离通过，发布来源切换为 `tenant`，最终 v2 恢复默认视觉。
- 真实公益凭证 `MPCG20260721-000155-B91B02EF` 返回 200、`image/svg+xml`，交易 `#155` 固化模板版本 0，后续发布不会漂移。
- 未登录访问后台模板列表返回 401。

## 浏览器验收

- 桌面端模板页面可见 5 类模板、编辑表单、成品预览、发布按钮和版本历史。
- 修改中文标题后 350ms 内实时刷新 SVG；预览画布 740×518，SVG 非空。
- 版本历史正确显示城市共建 v1/v2、发布说明和恢复入口。
- 390×844：文档宽度 375/375，无页面级横向溢出；5 类模板和 SVG 均加载。
- 桌面及移动验收控制台 warning/error 均为 0。

## 结论

统一证书模板自定义功能已形成从配置、权限、发布版本、发证快照、公开验真到多端展示的完整闭环，满足本地发布候选条件。
