# 05.06 客服工作台验收记录

## 验收结论

- 状态：通过。
- 租户客服账号：`showcase_support / Qiwai123456`。
- 权限：仅 `support.view`，PC 菜单仅显示客服查询台。
- 保留工单：`WO20260717CC7569E5`，记录 ID `5`，最终状态 `processing`。
- 保留用户：用户 ID `192`，授权查看手机号为 `13990063869`。

## 自动化与真实接口

- 新增 `npm run acceptance:support-work-order`，可重复创建/修复最小客服账号并执行完整验收。
- 默认检索、报名、订单、通知、验证码和工单响应中的手机号均为脱敏值。
- 空查看理由返回 400；填写理由后才返回完整手机号，并写入 `support.sensitive_reveal` 审计。
- 审计记录包含操作者、租户、用户、理由和请求上下文，不包含完整手机号。
- 可指派人员仅包含同租户、启用且具备 `support.view` 的账号。
- 租户客服读取平台工单 ID `1` 返回 404。
- 紧急工单 SLA 按数据库 `createdAt + 2 hours` 生成，避免应用与数据库时区不一致造成偏移。
- 状态链路已执行：`assigned -> processing -> waiting_user -> processing -> resolved -> closed -> processing`。
- 首次回复时间已生成，重开后关闭/解决时间清空，7 条处理轨迹保留且不可覆盖。

## 浏览器验收

- 使用 `showcase_support` 登录 `http://127.0.0.1:5174/admin/support`。
- 菜单仅显示客服查询台，未授权的会员、报名、订单快捷入口不显示。
- 搜索 `13990063869` 命中用户、报名、订单和通知；页面不存在 11 位完整手机号。
- 工单列表仅显示 tenant `23` 数据，用户手机号为 `139****3869`。
- 页面宽度与视口宽度均为 `1164`，无横向溢出；console warning/error 为 0。

## 验收中修复

1. 修复租户客服可读取平台工单的跨作用域越权。
2. 修复客服报名聚合响应返回完整手机号。
3. 修复最小客服账号仍显示无权限业务快捷入口。
4. 修复 SLA 直接由应用时间写入导致数据库时区偏移，改为数据库基准计算。
5. 演示租户后台账号额度由 10 调整为 30，并保留最小客服账号到可重复 seed。

## 回归结果

- API 全量：102 个测试文件、602 项通过。
- API 生产构建：通过。
- PC 生产构建：通过。
- 真实 API 容器：healthy。
- `git diff --check`：无空白错误，仅 Windows 换行提示。
