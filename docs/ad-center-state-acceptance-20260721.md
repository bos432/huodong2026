# 广告中心状态闭环验收（2026-07-21）

## 验收范围

- 页面：`/admin/ad-center`
- 账号：`showcase_admin`，租户 `#23 / 慢π演示中心`
- 覆盖广告计划、广告位、广告主、合同、结算、经营汇总的读取、失败恢复、写操作上下文和响应式状态。
- 保留全部既有业务与验收数据，故障演练仅停止并恢复 `activity-api`。

## 代码闭环

- 五个远程分区使用独立错误、独立重试、请求代次和范围快照；读取开始与失败时清空旧数据及总数。
- 写操作绑定记录、租户、筛选和列表代次，编辑抽屉与确认期间锁定顶部范围；既有广告主、合同和广告计划不可更改归属租户。
- 投放和结算导出绑定当前筛选，上传、保存、导出和状态操作互斥；结算状态流转复核原状态并二次确认。
- 权限分别控制管理、财务、敏感字段、导出和图片上传；无敏感权限时联系人手机号脱敏。
- 修复 Element Plus 复选框弃用告警：投放平台选值从旧 `label` API 改为 `value`，绑定值和显示文案不变。
- 新增 `ad-center-permission-contract.spec.ts`，固定失败清理、并发代次、目标保护、结算确认和交互锁定五项源码合同。

## 浏览器验收

正常态真实数据：

| 分区 | 结果 |
|---|---:|
| 投放计划 | 13 行 |
| 广告位配置 | 8 行 |
| 广告主管理 | 6 行 |
| 合同管理 | 10 行 |
| 结算对账 | 8 行 |
| 总曝光 / 总点击 / CTR | 13 / 2 / 15.38% |
| 自有广告收入 | 3.40 元 |
| 官方流量主收入 | 278.32 元 |
| 广告总收入 | 281.72 元 |

- 停止 API 后点击刷新，广告主、合同、广告计划、结算和经营汇总均显示 HTTP 502，并分别出现独立重试入口；远程旧数据清空，广告位本地元数据仍保留 8 行。
- API 恢复 healthy 后逐个点击五个重试入口，错误和入口消失，以上真实数量及经营汇总全部恢复。
- 打开首条广告计划编辑抽屉后，关键词、刷新、投放筛选和标签切换锁定；尝试切换合同标签仍停留投放计划，取消后抽屉关闭且未保存数据。
- 390x844 视口初验为页面 `375/375`；发布告警整改后新会话复验编辑抽屉为 `390/390`，控制台 warning/error 0。
- 截图保存在 `.local-logs/ad-center-state-20260721/`，最终恢复桌面视口并回到 `/admin/dashboard`。

## 自动化与运行态

- 广告中心专项：1 文件、5 项通过。
- PC 类型检查和 Vite 生产构建通过。
- 完整 `npm.cmd run ci:verify` 173.2 秒退出 0；运行时依赖 high/critical 为 0，全部 API 测试、preflight、Shared/API/PC/H5/微信小程序构建通过。
- `npm.cmd run monitor:health`：`status=ok`、`alerts=0`。
- 统一资金一致性：`healthy=true`、`issueCount=0`；活动订单 330、课程订单 91、商城订单 815、钱包流水 463、商城结算 26。
- readiness：`ready=true`、`blockingCount=0`；本地环境 20 项生产前或可选配置告警均不阻断开发验收。
- `activity-api`、`activity-mysql` healthy，`activity-nginx` running；`git diff --check` 退出 0，仅有既有 LF/CRLF 提示。
- 静态发布提示 API 缺少完整构建元数据，已作为本地 Docker 已知差异保留；不影响本批页面验收，正式发布仍由生产版本门禁复核。

## 结论

计划表 `11.01.68` 广告中心状态闭环通过。正式微信流量主、对象存储和真机广告组件继续归入既有生产外部验收项，不阻断 PC 后台本批完成。
