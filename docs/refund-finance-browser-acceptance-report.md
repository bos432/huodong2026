# 03.03 财务退款工作台浏览器验收

## 环境

- PC：`http://127.0.0.1:18080/admin/finance`
- API：Docker `activity-api`，`/api`，健康状态 `healthy`
- 租户：演示租户 `qiwai-showcase`（tenant `23`）
- 角色：`showcase_finance`

## 验收结果

| 场景 | 结果 | 证据 |
|---|---|---|
| 已完成并发退款展示 | 通过 | `CONCURRENCY_1784216696119a51cde_1`，状态“已完成”，金额 `40.00` |
| 失败退款展示 | 通过 | 受控记录 `UI_FAILED_REFUND_RESET_1784217600000`，显示服务商失败、失败原因和“重试” |
| 重试确认与锁 | 通过 | 打开“重试失败退款”确认框；确认后请求期间操作按钮锁定 |
| 重试状态一致性 | 通过 | 记录变为“已完成”，服务商退款号、状态、失败原因、同步时间、查询时间和载荷清空 |
| 退款回执扫描 | 通过 | 二次确认后执行，提示“已扫描 0 条退款回执”，无 500；无待查询记录时结果为 0 符合预期 |

## 回归

- API：93 个测试文件，550 项测试，全部通过。
- Preflight：全部通过；仅正式短信环境变量缺失警告，属于外部配置项。
- API 构建：通过；新镜像健康检查：通过。
- 测试数据保留，未清理退款、订单或账户。

## 待验收

正式微信/支付宝退款回执、独立 worker 并发积压与异地恢复仍需正式渠道和生产基础设施配置后执行。
