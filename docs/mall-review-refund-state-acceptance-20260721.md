# 商城评价与售后状态闭环验收报告

验收日期：2026-07-21  
工作包：`11.01.61`  
验收结论：通过。本批未执行评价审核、举报处置、售后审核或退款写操作，现有测试数据全部保留。

## 一、实现范围

- 商城评价、评价举报、售后申请和退款日志拆为四个独立读取分区，各自维护加载态、错误正文、旧数据清理和重试入口。
- 租户、店铺和关键词上下文均绑定请求代次；切换范围会立即清空旧数据，迟到响应不能覆盖当前页面。
- 评价审核与举报处置在确认后复核记录 ID、商品/评价、店铺、筛选上下文和列表代次。
- 售后通过、确认收货、换货发货、协商、退款重试和拒绝统一冻结售后单、订单、店铺和列表代次，确认后目标变化则拒绝提交。
- 写操作期间锁定租户、店铺及业务筛选，避免确认弹窗打开期间切换到另一范围。
- 范围及分区错误使用显式正文，避免 Element Plus 默认插槽遮挡接口错误。

## 二、浏览器正常态

账号：`showcase_admin`，租户 `qiwai-showcase`，店铺 `#38 / 慢π自营店`。

| 页面 | 真实读取结果 |
| --- | --- |
| 商城评价 | 已展示评价 19 条、评价举报 5 条、带图评价 19 条 |
| 商城售后 | 售后申请 42 条、待处理 21 条、退款日志 18 条、已完成退款金额 1036.75 元 |

两个页面桌面端均无加载错误，控制台 warning/error 为 0。手机号继续按页面既有规则脱敏。

## 三、故障与恢复

受控停止 `activity-api`，未停止 MySQL、未执行 migration、未修改评价、举报、售后、退款或订单状态：

- 评价刷新后 19 条评价与 5 条举报全部清空，六项评价汇总归零。
- 评价列表和举报列表分别显示 HTTP 502 正文，并提供“重试评价列表”和“重试举报列表”。
- 售后刷新后 42 条售后与 18 条退款日志全部清空，五项售后汇总归零。
- 售后申请和退款日志分别显示 HTTP 502 正文，并提供“重试售后申请”和“重试退款日志”。
- API 恢复为 healthy 后，两页真实数量完整恢复，无旧数据残留或跨分区覆盖。

## 四、移动端验收

390x844 视口下浏览器实际内容宽度为 375px：

- 评价页 `clientWidth=scrollWidth=375`，内容区 351/351，标题 323x31px。
- 售后页 `clientWidth=scrollWidth=375`，内容区 351/351，标题 323x31px。
- 标题、筛选、店铺信息、快捷操作和首屏统计无重叠，无页面级横向溢出。

截图：

- `.local-logs/mall-review-refund-state-20260721/mobile-mall-reviews-390x844-viewport.png`
- `.local-logs/mall-review-refund-state-20260721/mobile-mall-refunds-390x844-viewport.png`

## 五、自动化与运行态

| 检查 | 结果 |
| --- | --- |
| `menu-integrity.spec.ts` | 28/28，通过四分区、请求代次、旧数据清理和写目标合同 |
| API 全量测试 | 158 个文件、914 项全部通过 |
| `npm run ci:verify` | 130.5 秒退出 0 |
| 全端构建 | Shared、API、PC、H5、微信小程序全部通过 |
| 运行时依赖审计 | high=0、critical=0 |
| `npm run monitor:health` | `status=ok`、`alerts=0` |
| 统一资金一致性 | `healthy=true`、`issueCount=0`；活动 539、课程 93、商城 815、钱包流水 471、结算 26 |
| `/api/health/ready` | `ready=true`、`blockingCount=0` |
| `git diff --check` | 退出 0，仅有工作区既有换行提示 |

## 六、结论

商城评价、评价举报、售后申请和退款日志的读取真实性、范围竞态、写操作目标、错误恢复及移动布局已经闭环。下一工作包继续 `11.01` 中商城结算、统计及其他 PC 长尾页面状态治理。
