# 活动评价与举报状态闭环验收（2026-07-21）

## 范围

- 页面：`/admin/reviews`
- 验收账号：`showcase_admin / Qiwai123456`
- 补齐活动选项、评价列表和待处理举报三类读取的失败清空、请求代次、范围快照、响应校验与独立重试。
- 补齐精选、显示/隐藏和举报处置的写目标、状态、筛选及列表代次保护。
- 保留既有 `review.view/manage/sensitive` 三权分离、手机号脱敏、服务端事务锁、活动范围和审计规则。
- 本轮仅打开举报驳回提示并取消，未执行评价或举报写入。

## 实现

- 活动选项、评价列表和举报列表使用独立 loading、持久错误及请求代次；请求开始、结构异常或失败时分别清空旧选项、旧行和 total。
- 评价响应绑定活动、状态和页码快照，举报响应绑定 pending 状态和页码快照；迟到响应不能覆盖当前范围。
- 首次加载通过 `Promise.allSettled` 并行读取三分区，任一失败不遮蔽其他错误边界。
- 精选、显示/隐藏和举报处置在弹窗或请求前冻结目标 ID、原状态、关联评价、筛选及列表代次，写入前再次复核。
- 任一读取、确认或写入期间锁定活动/状态筛选、分页、重试及评价和举报的全部行操作。
- 故障时表格明确显示“评价加载失败/举报加载失败”，不再把请求失败误报为“暂无数据”。

## 浏览器验收

- 正常态：45 个活动选项、6 条评价、2 条待处理举报；页面共识别 8 处脱敏手机号，未出现明文敏感信息。
- 对举报“浏览器待处理举报验收 20260716122208”打开驳回提示后，活动/状态筛选、两类分页和全部评价/举报操作均锁定；点击取消后仍为 `Total 6 / Total 2`，未产生写入。
- 停止 `activity-api` 并重载后，旧评价、旧举报和分页总数立即清空；Nginx 超时后三分区分别持久显示 HTTP 502，评价和举报表格分别显示加载失败。
- API 恢复 healthy 后依次点击“重试活动选项”“重试评价”“重试举报”，45 个活动选项、6 条评价和 2 条举报完整恢复。
- 390x844 下 `clientWidth=scrollWidth=375`，两张业务卡片均为 `325/325`，标题 96x31，控制台 warning/error 0。
- 截图：`.local-logs/review-state-20260721/mobile.png`。
- 最终恢复 `showcase_admin` 工作台，桌面文档无横向溢出，控制台 warning/error 0。

## 自动化与运行态

- PC `vue-tsc` 和生产构建通过，1947 个模块，最终构建耗时 39.3 秒。
- 专项 2 文件、8 项通过。
- 完整 `ci:verify` 171.8 秒退出 0；运行依赖 high/critical 0，全部测试、preflight 和 Shared/API/PC/H5/微信小程序构建通过。
- 监控 `status=ok`、`alerts=0`；readiness `ready=true`、`blockingCount=0`。
- 统一资金 `healthy=true`、`issueCount=0`；活动订单 539、课程订单 93、商城订单 815、钱包流水 471、商城结算 26。
- API/MySQL healthy，Nginx running；完整 `git diff --check` 退出 0，仅有既有 LF/CRLF 提示。

## 结论

计划表 `11.01.80` 活动评价与举报状态闭环通过。既有计划 `11.01.36` 的权限账号、评价、举报、并发结果和审计数据全部保留。
