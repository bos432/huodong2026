# 06.01-06.03 经营分析体系验收记录

## 验收结论

- `06.01 指标口径、任务和重算`：通过。
- `06.02 多级经营看板与明细`：通过。
- `06.03 漏斗、留存、复购、渠道和地域`：通过。
- 可重复脚本：`npm run acceptance:analytics-governance`。

## 指标重算与一致性

- 保留运行编号：`AR1784254043898BD6A2C`。
- 日期范围：`2026-07-15` 至 `2026-07-17`。
- 来源事件：379。
- 指标行：77。
- 差异：0。
- 一致性：true。
- 计算版本：`activity-metrics-v1`。
- 指标行均保存运行批次和计算版本，可重复重算且不产生重复汇总。

## 平台与租户范围

- 平台新增用户：10,720；租户 `23` 新增会员档案：163。
- 平台与租户的活跃、等级、活动偏好和增长人群均返回不同结果。
- 租户账号附加 `tenantId=42` 请求参数后，响应与自身租户结果完全一致，不能切换到其他租户。
- 租户会员等级分布仅统计 `tenant:23` 档案，不再混入平台档案。
- 重复参与用户按当前日期和租户范围分组，不再使用全平台次数判断租户复购。

## 多级看板与导出

- 平台四业务域真实明细：活动 33、课程 3、商城 4、公益 10。
- 四业务域 CSV 均返回真实 `text/csv` 原始内容。
- 租户增长 CSV：928 bytes；平台指标 CSV：8,792 bytes。
- 修复统一响应拦截器把 CSV 包成 JSON 的问题；分析导出和课程成绩导出改为原生 Response 发送。
- 增加 CSV 控制器合同测试，防止回退为 JSON 包装下载。

## 浏览器验收

- 平台管理员打开 `/admin/analytics`，可见运行 `AR1784254043898BD6A2C`、77 行指标、差异 0。
- 页面展示统一日指标、趋势、风险、商家排行、渠道、会员等级、活动偏好和活动/课程/商城/公益四域入口。
- `showcase_finance` 打开 `/admin/funnels`，可见综合漏斗、留存、复购、来源、渠道和地域空态。
- 两页宽度均为 1164，视口宽度 1164，无横向溢出；console warning/error 为 0。

## 验收中修复

1. 租户新增用户原先使用全平台 users 计数，改为当前租户会员档案。
2. 会员等级分布原先未过滤 tenantScopeKey，改为平台/租户独立统计。
3. 重复参与原先以全平台报名次数判断，改为当前租户和日期范围内分组。
4. 分析 CSV 与课程成绩 CSV 原先被统一响应拦截器包装成 JSON，改为原始文件响应。

## 回归结果

- API 全量：103 个测试文件、604 项通过。
- API 生产构建：通过。
- PC 生产构建：本批前序构建通过，分析页面浏览器复验通过。
- Docker API：healthy。
