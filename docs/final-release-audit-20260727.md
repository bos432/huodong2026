# 最终发布审计报告（2026-07-27）

## 结论

- 当前候选版本未发现 P0/P1 代码阻断问题，可以进入部署阶段。
- 本轮修复了 2026-07-27 新增安全公告影响的 API 与管理后台生产依赖链；API、管理后台和 H5 的生产依赖审计均为 0 个已知漏洞。
- 未删除、重置或迁移任何业务数据。本地 195 项数据库迁移全部为已执行状态，没有待执行迁移。
- 线上仍运行旧版本 `b7d28864`，有 560 条到期业务任务积压。候选版本已包含业务任务消费者默认启动修复；部署后必须观察该指标持续下降至 0。
- 线上对象存储仍为本地模式，是正式扩容和容灾前的运维整改项，不阻断本次代码发布。

## 本轮整改

1. 管理后台锁定 `postcss 8.5.23`，消除新披露的高危公告。
2. API 保持 TypeORM `0.3.31`，覆盖其受影响的 `glob`、`minimatch` 和 `brace-expansion` 依赖链。
3. 覆盖 ExcelJS 的 `archiver 8.0.0` 与 `unzipper 0.12.3` 依赖链。
4. 使用干净 `npm ci` 和 Docker 生产镜像重复验证锁文件，生产镜像安装结果为 0 vulnerabilities。
5. 未采用 TypeORM 1.x；实验升级会造成 112 个编译兼容错误，已明确排除在本次发布之外。

## 自动化验证

- API 编译：通过。
- API 测试：176 个测试文件、1010 项测试全部通过。
- 四端构建：共享包、API、管理后台、H5、微信小程序全部通过。
- 微信小程序产物：授权页配置补丁写入成功。
- 发布门禁：44 项业务、权限、隐私、迁移、安全、备份、监控和回滚检查全部通过。
- 权限目录：144 项。
- 密钥扫描：1362 个跟踪或未忽略源码文件通过。
- 生产依赖审计：API、管理后台、移动端均无已知漏洞。
- ExcelJS：中文 XLSX 写入和读取 roundtrip 通过。
- 差异格式检查：通过，仅有 Windows LF/CRLF 转换提示。

## 运行与浏览器验收

- 本地容器：API、MySQL healthy，Nginx running；保留原 MySQL 数据卷。
- 本地健康指标：API、数据库、运营指标均为 1；到期任务、死信、锁超时、支付回调失败、退款服务商失败、库存异常和资金风险均为 0。
- H5 390x844：首页、专题、共修、活动、我的、公益池均正常；底部导航每次直接进入均显示；无坏图、无页面横向溢出、无控制台 warning/error。
- 管理后台桌面端：数据看板、装修、公益、证书模板、小程序发布、业务任务、活动、订单、会员均正常加载。
- 管理后台 390x844：数据看板、装修、公益、证书模板无坏图和页面横向溢出。
- 装修实时预览、公益分页分类、证书模板素材与姓名显示方式、小程序发布指引均正常呈现。
- 线上只读复验：H5、后台、API 可用，TLS/HSTS 与基础安全响应头正常；重点后台页面无坏图、横向溢出或控制台错误。

## 发布后必验

1. 确认线上 API、H5、后台 `version.json` 均为本次新提交号。
2. 确认 `/api/health/ready` 的 API 和数据库均为 up，blockingCount 为 0。
3. 每 30 秒查看 `/api/health/metrics`，确认 `activity_business_jobs_due` 从 560 持续下降并最终为 0，且死信和锁超时保持 0。
4. 重新完成 H5 首页、活动列表/详情、底部导航、后台装修、公益、证书和小程序发布页的线上浏览器验收。
5. 本次包含微信小程序前端产物相关提交，服务器部署不会自动更新已发布小程序；如需让小程序使用本次代码，仍需用微信开发者工具重新上传、提交审核和发布。

## 已知隔离风险

- `miniprogram-ci 2.1.31` 是微信官方当前最新版，但其开发依赖树仍包含旧 Babel、request、Jimp 等历史公告。正常 API 请求路径不会加载它，只有后台触发上传体验版时才动态加载。
- API 生产依赖审计为 0；完整开发依赖审计的告警主要来自上述微信发布工具。当前优先使用微信开发者工具上传，中期应把自动上传能力拆为独立、低权限的发布工具或容器。
- Element Plus 主资源和 H5 个别分包仍有体积提示，属于 P2 性能优化，不影响正确性和本次发布。
