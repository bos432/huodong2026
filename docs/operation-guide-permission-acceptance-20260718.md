# 运维教程权限与数据库目录验收报告

验收日期：2026-07-18

验收页面：`http://127.0.0.1:18080/admin/operation-guide`

## 1. 验收范围

- 平台查看与管理权限分层。
- 七个教程标签、生产命令和 PM2 路径。
- API 实体数据库目录完整性、中文质量、搜索和分类筛选。
- 普通命令复制、高风险命令确认取消和操作互斥。
- 标签位置按账号隔离。
- 桌面与 390×844 移动布局、外层横向溢出和控制台错误。

## 2. 测试账号

| 角色 | 账号 | 密码 | 权限用途 |
| --- | --- | --- | --- |
| 平台只读运维 | `showcase_system_settings_read` | `Qiwai123456` | `system.view`，只查看教程 |
| 平台运维管理 | `showcase_system_settings_manager` | `Qiwai123456` | `system.view`、`system.manage`，可复制命令 |

账号与浏览器本地状态均保留。只读账号保留“数据库”标签，管理账号保留“数据库”标签，二者存储键独立。

## 3. 功能结果

| 编号 | 场景 | 预期 | 实际 | 结果 |
| --- | --- | --- | --- | --- |
| OG-01 | 只读账号进入教程 | 页面可达，显示只读提示，不显示复制按钮 | 页面可达，复制按钮 0 | 通过 |
| OG-02 | 管理账号进入教程 | 不显示只读提示，显示复制按钮 | 复制按钮 32 | 通过 |
| OG-03 | 教程标签 | 七个教程标签均可达 | 七个标签均渲染 | 通过 |
| OG-04 | PM2 路径 | 仅使用实际安装路径 | 正确路径可见，旧路径不可见 | 通过 |
| OG-05 | 数据库目录 | 与全部 API 实体一致 | `197/197`，实体与目录完全一致 | 通过 |
| OG-06 | 中文质量 | 不出现机器拼接英文 | 未翻译大写词元 0，H5/SKU 为允许缩写 | 通过 |
| OG-07 | 典型表名 | 地址、资金告警、广告主、首页发布自然可读 | 四项均显示自然中文 | 通过 |
| OG-08 | 关键词搜索 | 搜索 `mall_refunds` 仅返回目标表 | 显示 `1/197` | 通过 |
| OG-09 | 高风险复制取消 | 弹确认框，取消不复制且按钮恢复 | 哨兵不变、确认按钮 0、按钮可用 | 通过 |
| OG-10 | 普通复制 | 无确认框，显示成功反馈 | 显示“命令已复制” | 通过* |
| OG-11 | 账号位置隔离 | 两个账号互不继承标签位置 | 只读账号数据库、管理账号初次常用命令 | 通过 |
| OG-12 | 桌面宽度 | 页面无外层横向溢出 | `999/999` | 通过 |
| OG-13 | 390×844 宽度 | 页头、筛选、表格不被外层裁切 | 页面 351px，页头/筛选 311px，文档 `375/375` | 通过 |
| OG-14 | 手机筛选 | 搜索和目标表可读 | `mall_refunds` 显示 `1/197` | 通过 |
| OG-15 | 控制台 | 本批不新增 warning/error | 最终近 5 分钟新增 0 | 通过 |

\* 应用内浏览器把页面脚本剪贴板与自动化控制剪贴板隔离。页面现代 Clipboard API 被拒绝后，兼容路径完成聚焦、选区和 `execCommand("copy")` 并返回成功；自动化侧再次粘贴仍只能读到其自身哨兵，因此无法用该浏览器的外部剪贴板 API 交叉读取页面剪贴板。正式交付浏览器应在 Chrome/Edge HTTPS 环境补一次人工粘贴确认。

## 4. 自动化与构建

- 运维教程契约与菜单：2 文件，25 项通过。
- API 全量：125 文件，685 项通过。
- PC 生产构建：1946 模块通过。
- 权限目录：90 项通过。
- 完整 `npm run preflight` 通过。
- 数据库目录 guard：197 张实体表一致，未翻译英文门禁通过。

## 5. 已执行整改

1. 生成器由整表名单数化改为逐词元规范化，补业务词典和特殊表名覆盖。
2. 新增生成目录英文残留门禁与典型中文标签契约。
3. 普通复制统一使用公共剪贴板工具，并增加权限拒绝后的兼容复制路径。
4. 修复移动端标签页最小内容宽度撑开、页头标签和筛选区被裁切的问题。
5. 把响应式关键 CSS、复制降级和正确 PM2 路径加入 preflight，防止后续回退。

## 6. 外部待验收

- 正式 Chrome/Edge HTTPS 环境人工粘贴一次普通命令，确认操作系统剪贴板内容。
- 正式服务器按教程执行前先完成数据库备份；本批只验证教程、权限和复制交互，不执行生产迁移、恢复或回滚命令。
