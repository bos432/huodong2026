# 部署文档 - 2026-07-06

## 部署版本

| 项目 | 值 |
| --- | --- |
| Git 分支 | `feature/qiwai-ui-experiment` |
| 目标提交 | `d736f197` |
| 宝塔站点目录 | `/www/wwwroot/rd.chaimen666.com` |
| 小程序导入目录 | `apps/mobile/dist/build/mp-weixin` |

## 宝塔服务器部署命令

在宝塔终端执行：

```bash
cd /www/wwwroot/rd.chaimen666.com
export PATH=/www/server/nodejs/v22.22.3/bin:$PATH

git fetch origin feature/qiwai-ui-experiment
git pull --ff-only origin feature/qiwai-ui-experiment

npm install --prefix packages/shared
npm install --prefix apps/api
npm install --prefix apps/admin
npm install --prefix apps/mobile --legacy-peer-deps

npm run build
npm --prefix apps/mobile run build:mp-weixin
```

## 写入 API 版本元数据

这一步用于修复 `/api/health/ready` 显示旧 commit 的问题。

```bash
COMMIT=$(git rev-parse --short HEAD)
BUILD_TIME=$(date -Iseconds)

set_env() {
  FILE="$1"
  KEY="$2"
  VALUE="$3"
  touch "$FILE"
  if grep -q "^${KEY}=" "$FILE"; then
    sed -i "s|^${KEY}=.*|${KEY}=${VALUE}|" "$FILE"
  else
    echo "${KEY}=${VALUE}" >> "$FILE"
  fi
}

set_env ".env" BUILD_COMMIT "$COMMIT"
set_env ".env" BUILD_TIME "$BUILD_TIME"
set_env "apps/api/.env" BUILD_COMMIT "$COMMIT"
set_env "apps/api/.env" BUILD_TIME "$BUILD_TIME"
set_env ".env" API_PORT 18080
set_env "apps/api/.env" API_PORT 18080

pm2 restart activity-api --update-env
```

## Nginx 检查

确认 Nginx 反代到 API 端口 `18080`：

```bash
CONF=/www/server/panel/vhost/nginx/rd.chaimen666.com.conf
grep -n "127.0.0.1:18080" "$CONF"
nginx -t
/etc/init.d/nginx reload
```

## 部署后验收

```bash
curl -s https://rd.chaimen666.com/api/health/ready
curl -s https://rd.chaimen666.com/admin/version.json
curl -s https://rd.chaimen666.com/version.json
pm2 status
```

期望：

| 检查 | 期望 |
| --- | --- |
| API ready | `ready=true` |
| API commit | `d736f197` 或服务器实际 `git rev-parse --short HEAD` |
| Admin version | `d736f197` |
| H5 version | `d736f197` |
| PM2 | `activity-api online` |

## 微信小程序发布

1. 在服务器或本地执行小程序构建：

```bash
npm --prefix apps/mobile run build:mp-weixin
```

2. 微信开发者工具导入目录：

```text
E:\2027\AI全自动开发1.0\活动报名-重编排工作副本\apps\mobile\dist\build\mp-weixin
```

服务器对应目录：

```text
/www/wwwroot/rd.chaimen666.com/apps/mobile/dist/build/mp-weixin
```

3. 开发者工具内测试：

| 流程 | 账号 |
| --- | --- |
| 普通用户登录、报名、查看签到码 | 测试手机号或微信体验号 |
| 手机管理端登录 | `showcase_admin / Qiwai123456` |
| 活动保存/发布 | `showcase_admin / Qiwai123456` |
| 签到核销页 | `showcase_checkin / Qiwai123456` |

4. 测试通过后上传体验版，再提交审核。

## 回滚方式

如部署后发现阻塞问题：

```bash
cd /www/wwwroot/rd.chaimen666.com
git log --oneline -5
git checkout <上一稳定commit>
npm run build
pm2 restart activity-api --update-env
nginx -t && /etc/init.d/nginx reload
```

回滚前建议先备份数据库和当前目录。
