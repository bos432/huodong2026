# 商城营销中心状态治理验收报告

## 范围

- 页面：`/admin/mall-marketing?tenantId=23&merchantId=38`
- 账号：`showcase_admin`，运营角色
- 店铺：慢π自营店，店铺 ID `38`
- 验收日期：2026-07-21

## 实现结果

- 店铺、商品分类目录、优惠券、使用记录、秒杀、拼团、参团记录、推广代理、推广码、风控事件、风险告警和佣金规则均使用独立请求代次。
- 刷新开始和读取失败时清除对应旧数据；迟到响应不能覆盖当前租户、店铺、筛选或标签。
- 平台商品目录读取失败与店铺目录失败分流。运营账号无平台商品数据范围时，店铺券正常态不显示全局错误；仅切换到平台券时提示平台范围不完整。
- 启停、风险告警处置和佣金规则停用均在确认后复核目标记录、店铺、筛选和列表代次；新建与编辑保存复核原表单上下文。

## 浏览器验收

| 场景 | 结果 |
|---|---|
| 正常读取 | 优惠券 6、秒杀 1、拼团 48、推广码 2 |
| 店铺券目录权限 | 无“店铺营销商品或分类加载失败”误报 |
| API 中断 | 四项汇总归零，`ONCE5`、`SHOWCASE10` 等旧券码消失，各分区显示 HTTP 502 和重试入口 |
| API 恢复 | 四项汇总及旧有业务数据完整恢复 |
| 写操作上下文 | 打开 `BOUNDARY0716055523` 启用确认时范围控件锁定；点击取消，优惠券仍为停用 |
| 移动布局 | 390x844 视口下文档 `clientWidth=375`、`scrollWidth=375`，标题 323x31px |
| 控制台 | warning 0，error 0 |

截图：`.local-logs/mall-marketing-state-20260721/mobile-390x844.png`。

## 自动化与运行状态

- `menu-integrity.spec.ts`：31/31。
- PC 生产构建：通过。
- `npm run ci:verify`：131 秒，退出 0；API 全量测试、全部 preflight、Shared/API/PC/H5/微信小程序构建通过。
- 运行时依赖审计：high 0，critical 0。
- 监控：`status=ok`，`alerts=0`。
- 统一资金：`healthy=true`，`issueCount=0`。
- readiness：`ready=true`，`blockingCount=0`。
- Docker：API、MySQL 均为 healthy。
- `git diff --check`：退出 0，仅存在既有行尾转换提示。

## 未执行的外部验收

本工作包不涉及真实支付或微信真机写操作。正式支付、微信小程序和外部消息通道仍按生产配置清单统一终验。
