# H5 验证码日志状态闭环验收（2026-07-21）

## 范围

- 页面：`/admin/h5-code-logs`
- 平台验收账号：`admin`
- 补齐日志失败清空、请求代次、筛选快照、响应校验与导出范围锁定。
- 未触发验证码发送，未执行日志导出，未修改或删除安全日志。

## 实现

- 日志读取绑定手机号、状态、模式筛选快照和请求代次，拒绝迟到响应。
- 请求开始、响应异常或失败时清空旧行、总数和成功/失败/限流汇总。
- 校验 `items/total/summary` 响应结构后才渲染。
- 导出冻结当前手机号、状态和模式筛选；导出期间锁定刷新、筛选、查询、重置和重试。
- 手机号、IP、服务商消息号的既有权限与脱敏规则保持不变。

## 浏览器验收

- 正常态：278 条，发送成功 274、失败 2、限流 2。
- 桌面 `1066/1066`，控制台 warning/error 0。
- 停止 API 后刷新，日志行和四项汇总全部归零；故障恢复过程未保留旧日志结论。
- API 恢复后完整回归为 278 条及 `274/2/2`。
- 390x844 为 `clientWidth=scrollWidth=375`，标题高度 31px，控制台 warning/error 0。
- 截图：`.local-logs/h5-code-log-state-20260721/mobile.png`。
- 最终恢复 `showcase_admin` 工作台 `1081/1081`，控制台 warning/error 0。

## 自动化与运行态

- PC 类型检查和生产构建通过。
- 专项 3 文件、39 项通过。
- 完整 `ci:verify` 158.7 秒退出 0；运行依赖 high/critical 0，全部测试、preflight 和 Shared/API/PC/H5/微信小程序构建通过。
- 监控 `status=ok`、`alerts=0`；readiness `ready=true`、`blockingCount=0`。
- 统一资金 `healthy=true`、`issueCount=0`；活动 330、课程 91、商城 815、钱包流水 463、结算 26。
- API/MySQL healthy，Nginx running；差异检查无空白错误。

## 结论

计划表 `11.01.75` H5 验证码日志状态闭环通过。正式短信服务商和真实短信回执仍按生产配置终验。
