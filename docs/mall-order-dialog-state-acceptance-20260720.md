# 商城订单详情、发货及营销弹窗状态验收报告

验收日期：2026-07-20  
页面：`/admin/mall-orders?tenantId=23&merchantId=38`  
验收账号：`showcase_admin / Qiwai123456`  
验收店铺：`38 / 慢π自营店`

## 一、整改范围

- 订单详情不再在详情接口失败时静默回退到列表行；详情失败清空内容并显示持久错误。
- 详情请求捕获订单 ID 和请求代次；关闭抽屉、切换跨店子单或打开另一订单后，迟到响应不能写回。
- 跨店子单列表独立清空、报错和重试，同时复核当前详情订单及结算组号。
- 发货弹窗使用独立 `shipOrderTarget`，不再与详情抽屉共用可变 `currentOrder`；发货提交前再次复核订单 ID。
- 发货物流选项与物流配置列表拆分，分别绑定发货订单所属店铺和物流配置弹窗当前店铺。
- 店铺或租户范围变化时关闭全部订单、发货、物流及营销弹窗，并使旧请求全部失效。
- 优惠券适用范围、优惠券、使用记录、秒杀、拼团、参团记录、推广代理和推广码均增加请求代次、旧数据清理、响应校验、持久错误和独立重试。
- 推广代理接口兼容分页对象 `{ items, total, page, pageSize }`，使用 `pageSize=100` 完整加载当前 39 个代理。
- 关键读取错误未恢复时，发货、物流和营销新增/保存按钮禁用。

## 二、正常态浏览器验收

- 订单 `MO178423165292480EF14` 详情显示余额支付、商品金额 59 元、优惠 5 元、实付 54 元及完整收货地址。
- 订单商品为“【演示】慢π读书手账 / 礼盒款 × 1”。
- 发货弹窗继续显示同一订单号、同一收货地址及未发/总数 `0/1`；取消弹窗，没有创建包裹或修改物流。
- 慢π自营店物流设置读取顺丰演示、中通演示、京东演示 3 条配置。
- 推广码弹窗读取既有推广数据，代理选择完整显示 39 项。
- 390x844 下详情抽屉宽 378px，`clientWidth=scrollWidth=378`，内容未出现横向溢出。
- 浏览器最终控制台 warning/error 0。

截图：`.local-logs/mall-order-dialog-state-20260720/mobile-detail-390x844.png`

## 三、故障与恢复

受控停止 `activity-api`，未停止 MySQL、未执行 migration、未修改订单和发货数据：

- 打开订单详情后只显示持久 HTTP 502 错误，不显示列表行残片，也不出现原订单号。
- API 恢复后点击“重新加载订单详情”，订单号、商品、金额和地址全部恢复。
- 推广码弹窗故障时代理和推广码列表同时清零，分别显示独立 502 错误，新增按钮禁用。
- 正常态重新打开推广码弹窗后，既有推广数据和 39 个代理选项完整恢复。

## 四、自动化与运行态

| 检查 | 结果 |
| --- | --- |
| `menu-integrity.spec.ts` | 25/25，通过详情、发货、物流和营销弹窗状态合同 |
| API 全量测试 | 158 个文件、911 项全部通过 |
| `npm run ci:verify` | 165.9 秒，退出 0；运行时 high/critical 0，全部 preflight 与四端构建通过 |
| PC 生产构建 | 1947 modules，通过 |
| `npm run monitor:health` | `status=ok`、`alerts=0` |
| 统一资金一致性 | `healthy=true`、`issueCount=0` |
| `/api/health/ready` | `ready=true`、`blockingCount=0` |
| `git diff --check` | 退出 0 |

## 五、结论

商城订单工作台的核心读取、详情、跨店子单、分包发货、物流及内嵌营销弹窗状态治理已闭环。下一工作包继续独立商城商品页面及其 SKU、审核、库存和批量操作的长尾状态审计。
