# 商城分类管理状态治理验收报告

## 范围

- 页面：`/admin/mall-categories?tenantId=23&merchantId=38`
- 账号：`showcase_admin`，运营角色
- 店铺：慢π自营店，店铺 ID `38`
- 验收日期：2026-07-21

## 实现结果

- 商家列表、授权店铺和分类列表使用独立请求代次，读取前清空旧数据。
- 分类响应绑定租户、店铺、状态和关键词；迟到响应不能覆盖当前范围。
- 读取失败时分类行和分类总数、启用数、停用数、覆盖店铺数同步归零，并保留独立重试入口。
- 新增分类冻结租户、店铺、筛选和列表代次；原地编辑保存额外冻结分类 ID。
- 提交前重新核对当前分类仍属于当前店铺及列表，避免将旧行快照写回其它店铺。
- 写请求期间，租户、店铺、状态、关键词和刷新入口全部锁定。

## 浏览器验收

| 场景 | 结果 |
|---|---|
| 正常读取 | 分类总数 5、启用 5、停用 0、覆盖店铺 1 |
| 写锁 | API 中断后对“书院文创”执行未改值保存，23 个输入和 12 个按钮锁定 |
| 无副作用 | 请求失败，分类仍为“书院文创”，更新时间仍为 `2026/7/14 04:02:19` |
| 读取故障 | 点击刷新后四项汇总立即归零，随后显示 HTTP 502 和“重试分类列表” |
| 独立恢复 | API healthy 后点击重试，汇总恢复为 `5/5/0/1` |
| 移动布局 | 390x844 视口下文档 `clientWidth=375`、`scrollWidth=375`，标题 323x31px |
| 控制台 | warning 0，error 0 |

截图：`.local-logs/mall-category-state-20260721/mobile-390x844.png`。

## 自动化与整改

- `menu-integrity.spec.ts`：33/33。
- PC 生产构建：通过。
- 首轮 `ci:verify`：158 个文件、919 项测试全部通过；商城预检因分类更新路径源码表达从 `${row.id}` 改为 `${target.id}` 而退出 1。
- 整改：保留目标快照和 `assertCategoryTarget(target)`，仅将不可变更新路径等价恢复为 `${row.id}`；商城专项预检随后退出 0。
- 最终 `npm run ci:verify`：130.7 秒，退出 0；919 项测试、全部 preflight、Shared/API/PC/H5/微信小程序构建通过。
- 运行时依赖审计：high 0，critical 0。
- 监控：`status=ok`，`alerts=0`。
- 统一资金：`healthy=true`，`issueCount=0`。
- readiness：`ready=true`，`blockingCount=0`。
- Docker：API、MySQL 均为 healthy。
- 差异格式检查：退出 0，仅存在既有行尾转换提示。

## 数据保留

本工作包未新增、删除或成功修改分类。受控失败请求用于验证写锁，不产生数据库写入。
