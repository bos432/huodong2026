# 退款补偿任务最终验收报告（2026-07-20）

## 1. 验收范围

- 活动、课程、商城退款的提交、查询、失败重试、死信和重放。
- Worker 停止期间的任务积压与重启恢复。
- 业务任务幂等、并发认领、商家隔离、管理权限和审计信息。
- 课程余额退款资金来源恢复、退款接口隐私边界和 PC 财务展示。

## 2. 实现结果

- 活动沿用 `refund.provider-query`；课程新增 `course-refund.provider-query`；商城新增 `mall-refund.provider-query`。
- 课程退款新增渠道状态、同步时间、脱敏前内部回执、查询次数和下次查询时间，通过 migration `1783970000000` 管理。
- 课程微信/支付宝真实模式提交原路退款并自动查询；余额、线下及沙箱模式在数据库事务内完成。余额退款按支付时的赠送金/现金来源恢复。
- 课程和商城任务使用 `tenantId + type + refundId` 稳定幂等键；任务 tenant、payload tenant、退款 tenant 三者不一致时拒绝执行。
- 渠道处理中继续失败并进入业务任务退避；渠道终态完成任务；达到最大次数进入死信，管理员可审计后重放。
- 课程退款后台接口改为白名单 DTO，只返回脱敏手机号和必要业务字段，不返回密码哈希、微信身份和渠道原始报文。
- PC 课程退款页展示渠道状态、渠道单号、失败原因、查询次数和下次查询时间；表格统一横向滚动，固定列不再覆盖内容。

## 3. 保留验收数据

| 对象 | 编号 | 最终状态 |
|---|---|---|
| 课程测试订单 | `#101 / CRJOB1784524888839` | 已关闭，保留受控失败原因 |
| 课程退款 | `#13 / CRFJOB1784524888839` | 已拒绝 |
| 商城退款 | `#74 / MRFJOB1784524888839` | 已拒绝 |
| 课程补偿任务 | `#611 / course-refund:13` | 死信重放后已完成 |
| 商城补偿任务 | `#612 / mall-refund:74` | 死信重放后已完成 |
| 跨租户任务 | `#613` | 待执行，商家 `#23` 不可见/不可操作 |

专项结果：`.local-logs/refund-business-job-1784524888503/result.json`。

## 4. 验收步骤与结果

1. 登录平台超管和商家业务任务管理员，确认两者权限范围不同。
2. 停止 `activity-api`，直接写入两笔处理中退款和两条到期任务；任务保持 `pending / attemptCount=0`。
3. 重启 API 并执行到期任务；课程和商城 Handler 均被真实调用，因正式支付挡板关闭而各尝试一次进入死信，并记录 Worker 和错误。
4. 商家管理员列表只能看到商家 `#23` 的 `#611/#612`，看不到 `#613`，跨租户取消返回 404。
5. 重复插入 `course-refund:13` 被唯一约束拒绝。
6. 将两笔受控退款置为已拒绝终态，重放死信并再次执行；两条任务均完成，尝试次数保持一次，没有重复退款。
7. 课程余额支付专项再次执行：6 个并发支付请求只扣款一次，两次部分退款恢复 250 元赠送金和 100 元现金，统一资金检查健康。
8. PC 业务任务页和课程退款页完成桌面、390x844 验收；页面级无横向溢出，控制台 warning/error 为 0。

## 5. 数据库与门禁

- 升级前备份：`backups/mysql/activity_registration-20260720-131530.sql.gz`。
- migration `1783970000000-CourseRefundProviderRecovery` 已完成主库 `up/down/up`，当前 190 条 migration 全部执行。
- API 全量：153 个测试文件、867 项通过。
- Shared、API、PC、H5、微信小程序构建通过；PC 1946 模块。
- 完整 `npm run preflight` 通过。
- Docker API/MySQL 均 healthy；readiness `ready=true`、`blockingCount=0`。
- 统一资金一致性 `healthy=true`、问题数 0。
- `git diff --check` 无空白错误，仅有 Windows 行尾提示。

## 6. 生产终验项

正式微信/支付宝退款必须在主体证书、商户号、回调域名和密钥配置完成后复核：真实提交、处理中查询、成功回调、明确失败、网络超时恢复和渠道账单到账。代码和生产挡板已完成，缺少正式服务商配置时不得把沙箱结果标记为真实渠道验收。
