# 08.04 内容治理验收记录

验收日期：2026-07-17

## 验收范围

- 社区关键词替换、拒绝发布、举报与重复举报。
- 禁言/禁用作用域、处罚到期、解除、申诉和通知。
- 租户隔离、并发幂等、PC 后台历史追溯和 H5 会员自助申诉。

## 保留数据

- 最新自动化会员：31013 / 13068974524。
- H5 浏览器会员：31010 / 13066970074。
- 验收动态：42。
- 最新自动化评论：53、54；处罚：18、19、20；申诉：17；举报：5。

## 自动化结果

- `npm run acceptance:content-governance` 通过。
- 自动替换规则把命中词替换为“[已过滤]”；精确拒绝规则返回 400。
- 杭州管理员删除演示中心关键词规则返回 404。
- 社区禁言在生效期阻止发布；人工解除后恢复。
- 已超过截止时间的处罚在读取/校验前自动归档为 `expired`。
- 同一幂等键 8 路并发申诉只生成一条记录。
- 杭州管理员审核演示中心申诉返回 404；本租户申诉通过后自动解除关联处罚。
- 重复举报返回 400；杭州举报列表不包含演示中心记录。
- 最新会员建立 `tenant:23` 会员档案，7 条处罚、解除、申诉和举报通知全部落库，状态为 `sent` 或按频控 `suppressed`，无 `failed`。

## 浏览器结果

- H5 显示“已解除”“已到期”处罚和已通过申诉，提交时间按 Asia/Shanghai 显示为 2026-07-17 13:42。
- PC 内容治理工作台显示关键词、举报、处罚和申诉四个页签。
- PC 修复只加载待处理申诉的问题，现可同时回看待处理、已通过和已驳回记录；只有待处理记录显示处理按钮。
- PC 与 H5 无横向溢出，console warning/error 为 0。

## 整改记录

- 增加处罚到期状态归档，避免截止后仍显示“生效中”。
- 内容申诉、社区举报、处罚和统一通知新记录显式使用应用时间，避免 MySQL 默认 UTC 与数据源 +08:00 混用。
- 治理通知前幂等建立当前租户会员档案，不放宽通知中心的租户目标校验。
- PC 申诉工作台改为加载全状态历史。

## 回归结果

- API 全量：114 个测试文件、622 项全部通过。
- shared、API、PC、H5 和微信小程序生产构建通过。
- API Docker 镜像重建并通过 ready 检查，MySQL 数据卷保持不变。

## 外部复核

- 正式短信、公众号/小程序订阅消息及微信真机通知授权仍待生产通道复核。
