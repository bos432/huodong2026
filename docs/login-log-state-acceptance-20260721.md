# 后台登录日志状态闭环验收（2026-07-21）

## 范围

- 页面：`/admin/admin-login-logs`
- 平台验收账号：`admin`
- 补齐日志与商家选项的请求代次、失败清空、响应校验，以及导出筛选冻结和交互锁定。
- 未执行日志导出，不删除或修改任何安全日志。

## 实现

- 日志列表绑定账号、状态和商家筛选快照及请求代次；迟到响应不能覆盖当前范围。
- 日志请求开始、结构异常或失败时清空旧行、总数和成功/失败/限流汇总。
- 商家选项独立增加请求代次、失败清空和响应结构校验。
- 导出冻结账号、状态和商家筛选；导出期间锁定刷新、筛选、查询、重置和重试入口。
- 权限化敏感字段与后端脱敏规则保持不变。

## 浏览器验收

- 正常态：3014 条，登录成功 2938、失败 76、限流 0；商家选项 94 个。
- 桌面 `1066/1066`，控制台 warning/error 0。
- 停止 API 后刷新，日志行及四项汇总归零并持久显示 HTTP 502。
- API 停止状态重载页面，商家选项和日志列表分别显示 502；旧商家选项、日志和汇总均未残留。
- API 恢复后页面完整回归为 3014 条及 `2938/76/0`。
- 390x844 为 `clientWidth=scrollWidth=375`，标题高度 31px，控制台 warning/error 0。
- 截图：`.local-logs/login-log-state-20260721/mobile.png`。
- 最终恢复 `showcase_admin` 工作台 `1081/1081`，控制台 warning/error 0。

## 自动化与运行态

- PC 类型检查和生产构建通过。
- 专项 3 文件、39 项通过。
- 完整 `ci:verify` 206 秒退出 0；运行依赖 high/critical 0，全部测试、preflight 和 Shared/API/PC/H5/微信小程序构建通过。
- 监控 `status=ok`、`alerts=0`；readiness `ready=true`、`blockingCount=0`。
- 统一资金 `healthy=true`、`issueCount=0`；活动 330、课程 91、商城 815、钱包流水 463、结算 26。
- API/MySQL healthy，Nginx running；差异检查无空白错误。

## 结论

计划表 `11.01.74` 后台登录日志状态闭环通过。历史安全日志和全部测试数据保持不变。
