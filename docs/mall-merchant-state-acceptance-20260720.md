# 商城店铺治理状态与故障恢复验收报告

验收日期：2026-07-20  
页面：`/admin/mall-merchants?tenantId=23`  
验收租户：`23 / 慢π演示中心（qiwai-showcase）`

## 一、整改范围

- 主店铺列表、租户/代理选项、支付就绪度、入驻申请、店铺授权、资质合同和收款账户均增加独立持久错误状态。
- 主列表、支付就绪度和入驻申请增加请求代次保护；刷新开始即清除旧数据，迟到响应不能覆盖新请求。
- 授权、资质合同和收款账户请求绑定当前店铺 ID，切换店铺后旧响应不能写回新弹窗。
- 资质与合同改用 `Promise.allSettled`，一个分区失败不会抹掉另一个成功分区。
- `/admin/agents` 同时兼容历史数组和当前分页对象 `{ items, total, page, pageSize }`，以 `pageSize=100` 拉取当前租户完整代理选项。
- 390px 移动视口下，页面标题、说明、筛选和操作区改为全宽纵向布局，避免标题与说明被筛选区挤成窄列。

## 二、浏览器成功态

平台管理员 `admin / Admin123456` 进入租户 23 的商城店铺治理页：

- 店铺总数 68，待配置 60，支付待联调 8，支付状态读取失败 0。
- 店铺 `09.01验收店铺749107` 的手机号按 `131****9107` 脱敏。
- 授权弹窗读取 2 条授权；资质/合同弹窗读取 3 条数据；收款账户显示真实空态。
- 新增店铺切换为代理店铺后，代理下拉显示完整 39 项；关闭弹窗且未保存，没有新增店铺。
- 桌面视口 `clientWidth=1265 / scrollWidth=1265`，390x844 视口 `clientWidth=375 / scrollWidth=375`。
- 页面恢复后控制台 warning/error 为 0。

截图：

- `.local-logs/mall-merchant-state-20260720/desktop-success.png`
- `.local-logs/mall-merchant-state-20260720/mobile-success-390x844.png`

## 三、故障与恢复

受控停止 `activity-api`，未停止 MySQL、未执行 migration、未修改测试数据：

- 收款账户弹窗显示持久 HTTP 502 错误、列表清零，并提供唯一“重新加载收款账户”入口。
- 主刷新后店铺列表清零，全部汇总归零；页面分别显示筛选选项同步失败和商城店铺加载失败。
- 旧店铺、旧支付状态、旧授权和旧治理数据均未残留。
- API 恢复 healthy 后，点击“重新同步选项”恢复 68 个店铺、60 个待配置和 8 个支付待联调，错误区域消失。

## 四、自动化与运行态

| 检查 | 结果 |
| --- | --- |
| `menu-integrity.spec.ts` | 24/24，通过商城治理状态合同和代理分页对象合同 |
| API 全量测试 | 158 个文件、910 项全部通过 |
| `npm run ci:verify` | 145.1 秒，退出 0；运行时 high/critical 0，全部 preflight 和四端构建通过 |
| `npm run monitor:health` | `status=ok`、`alerts=0` |
| 统一资金一致性 | `healthy=true`、`issueCount=0`；活动订单 539、课程订单 93、商城订单 815、钱包流水 471、结算单 26 |
| `/api/health/ready` | `ready=true`、`blockingCount=0` |
| `git diff --check` | 退出 0，仅有工作区既有 LF/CRLF 提示 |

## 五、结论

本批商城店铺治理页的分区失败、旧数据清理、请求竞态、代理选项契约、移动布局和故障恢复均已闭环。正式对象存储、正式支付通道和微信真机仍按生产配置清单执行最终外部验收，不影响继续整改其他 PC 长尾页面。
