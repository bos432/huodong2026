# 公益贡献凭证姓名展示整改验收（2026-07-21）

## 整改结果

- 本人登录下载继续显示完整昵称或姓名。
- 后台财务授权预览改为独立鉴权接口，显示完整昵称或姓名，并按管理员租户范围查询。
- 公开验真和公开图片继续脱敏；默认“用户+数字”昵称统一显示为 `用户***`，普通中文姓名按 `张*明` 形式展示，手机号按 `138****8000` 展示。
- 后台预览改为页面内图片弹窗，不再依赖可能被浏览器拦截的新窗口。

## 验收证据

- 专项测试：2 个文件、13 项通过。
- API 与后台 production build 通过。
- 真实凭证 `MPCG20260721-000155-B91B02EF`：公开图片 HTTP 200，显示 `用户***`，不包含完整昵称；后台鉴权图片 HTTP 200，显示完整昵称 `用户7776`；未登录访问后台图片返回 401。
- 缓存边界：公开图片 `public, max-age=300`，后台图片 `private, no-store`。
- 桌面后台弹窗图片 948×657，无页面级横向溢出；390×844 下弹窗宽 390、图片 302×211，无横向溢出。
- 浏览器控制台 warning/error 0，API 健康状态 `ok`。

