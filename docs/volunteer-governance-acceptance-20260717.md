# 10.04 志愿者档案、任务、签到与工时治理验收报告

## 验收结论

- 志愿者档案、任务报名、录取、签到签退、双确认工时、培训资格、勋章和服务证明已完成真实 MySQL、并发及 PC/H5 验收。
- 手机号采用加密存储、盲索引检索和脱敏展示；关键写操作使用业务键并发重放。
- 微信小程序产物已构建，微信开发者工具和真机权限仍按生产环境终验。

## 保留数据

- 志愿任务：`50`
- 任务报名：`48`
- 志愿者档案：`81`
- 服务记录：`42`
- 工时调整：`11`
- 培训资格：`1`
- 勋章：`33`
- 服务证明：`11 / VPR202607175e2853d5c4ee4209`，最终已撤销
- H5 验收会员：`13994664467 / Qiwai123456`

## 并发与状态机验收

```powershell
npm run acceptance:premium-roles
```

- 报名业务键重放只返回报名 48；无效状态跳转稳定返回 400。
- 录取、签到、签退、志愿者工时确认和运营工时确认均执行两路真实并发重放，只推进一次状态。
- 签退自动生成待志愿者确认工时；志愿者确认后进入待运营确认，运营确认后计入累计时长。
- 工时调整业务键重放只生成调整 11；累计时长、成长等级和勋章按已确认工时重算。
- 培训 1 保存完成时间和一年有效期，经审核后状态为 approved。
- 服务证明可公开脱敏验真；撤销后公开验真返回无效。

## 缺陷整改

- 修复并发签到/签退时第二请求在 Repeatable Read 快照中读不到首请求记录、状态已推进后错误返回 400 的问题；锁定报名后增加业务键当前读回放。
- 修复运营工时确认并发重放的同类问题；服务记录锁定后再次核对 `supervisorConfirmationKey`。
- 旧大使/伙伴线索生成的志愿档案不再显示 `[encrypted]` 迁移占位符；新档案从线索加密备注解密后重新加密保存。

## 浏览器验收

### PC

- 概览显示档案 81、已通过 72、待审核 9、累计服务 106.5 小时及证书待办。
- 最新档案显示脱敏手机号、绑定状态、1.3 小时服务时长，以及发证、培训、凭证和证书操作。
- 档案、服务记录和任务报名页签可用，控制台 warning/error 为 0。

### H5

- 志愿服务页显示会员档案、累计服务 1.3 小时、开放任务、任务报名和服务记录。
- 已完成报名状态正确，服务记录显示 confirmed；签到凭证、报名取消和工时确认控件已接入同一业务状态机。
- 临时 `city_partner` 权益验收后恢复为 `standard`，`volunteers=false`。
- 页面控制台 warning/error 为 0。

## 全量回归

- API：115 个测试文件、629 项测试通过。
- shared、API、PC、H5 与微信小程序构建通过。
- `git diff --check` 通过。

## 待生产终验

- 微信开发者工具和真机验证扫码/粘贴签到凭证、断网重试、相机权限及安全区适配。
- 正式场地验证签到窗口、定位策略、多设备并发和现场弱网恢复。
- 志愿者实名、培训证件和证明文件接入正式私有对象存储及合规留存策略。
