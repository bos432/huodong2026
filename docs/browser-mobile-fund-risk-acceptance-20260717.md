# 移动管理端资金异常浏览器验收

验收时间：2026-07-17

## 验收账号

- 处置账号：`showcase_finance / Qiwai123456`
- 只读账号：`showcase_agent_owner / Qiwai123456`
- 租户：慢π演示中心
- 页面：`http://127.0.0.1:5173/#/pages/admin/risk-alerts`

## 验收结果

| 项目 | 结果 |
|---|---|
| 权限拆分 | `finance.view` 映射 `canViewFinanceRisks`，`finance.manage` 映射 `canManageFinanceRisks`。 |
| 只读反例 | `showcase_agent_owner` 可读取告警列表，`canManageFinanceRisks=false`，扫描接口返回 403。 |
| 状态筛选 | 待处理、跟进中、已解决、全部四类状态可切换；当前待处理为空。 |
| 类型筛选 | 支付、回调、账实、账单、退款、钱包六类及全部类型可切换。 |
| 保留告警 | 已解决列表真实加载 9 条告警，包括支付回调失败、重复支付、退款失败、渠道账单差异和钱包负余额。 |
| 告警详情 | 展示级别、状态、标题、异常说明、业务编号、发现次数、最近发现时间、处理人和处理依据。 |
| 状态机 | 开放状态支持确认跟进和填写依据解决；已解决状态支持重新打开；解决操作必须填写处理依据。 |
| 操作保护 | 打开钱包负余额告警重新打开弹窗后取消，未提交状态变更；弹窗期间锁定其他处置，并校验 token 与租户未切换。 |
| 扫描 | 仅 `finance.manage` 展示和允许异常扫描；本次为保护保留证据未执行扫描。 |
| 响应式 | 390 x 844 视口无横向溢出；筛选使用受控横向滚动，告警卡片和底部导航无重叠。 |
| 控制台 | warning/error 为 0。 |

## 自动化证据

- 资金风险、菜单契约和后台服务专项：3 个测试文件、57 项通过。
- API、H5 和微信小程序生产构建通过。
- API 容器使用最新镜像重建并 ready，MySQL 容器和数据卷保持不变。

## 待外部验收

- 正式支付渠道制造的真实失败回调、退款失败和账单差异处置。
- 微信开发者工具及真机的长告警列表、弱网重试和弹窗输入验收。

