# 线上整改上线执行记录（2026-07-01）

## 1. 执行结论

- 本机代码侧已完成本轮上线整改实现与验证，具备提交到服务器执行部署的条件。
- 线上 `https://rd.chaimen666.com` 尚未部署本轮构建：API ready 仍显示 commit `9260d75`，本地构建产物为 `a6fffcb1`。
- 当前不能判定“正式上线验收通过”，原因是缺少服务器部署会话、腾讯云短信真实凭据、演示账号统一密码和小程序体验版验证环境。

## 2. 本轮补齐内容

- 短信服务：
  - 腾讯云短信适配保留 SDK AppID 必填校验。
  - `mock-sms` 在非生产/显式测试模式下可用于本地调试，生产环境会被拒绝假发送。
  - `SMS_SDK_APP_ID` 已同步到 `.env.production`、`.env.production.example`、Docker Compose、doctor、preflight、后台部署配置和上线检查清单。
  - 新增 `notification-provider.service.spec.ts` 覆盖 mock/生产/腾讯云缺字段边界。

- 构建与版本：
  - Admin/H5 构建后写入 `version.json`。
  - 后台部署配置页展示短信 SDK AppID，并纳入短信就绪度检测。

- 既有整改保留：
  - 签到页响应式修复。
  - 店铺/代理演示账号 seed 与角色验证脚本。
  - 营销弹窗生效检测与频次缓存清理。
  - 广告图片校验与前台 `resolvedImageUrl` 兜底。
  - 会员详情时间线、快捷筛选、批量标签。
  - 首页上线简洁版模板入口。

## 3. 已执行验证

| 项目 | 结果 |
| --- | --- |
| `npm --prefix apps/api run test` | 通过，14 个测试文件、172 个用例 |
| `npm --prefix apps/api run build` | 通过 |
| `npm --prefix apps/admin run build` | 通过，写入 `apps/admin/dist/version.json` |
| `npm --prefix apps/mobile run build:h5` | 通过，写入 `apps/mobile/dist/build/h5/version.json` |
| `VITE_API_BASE=https://rd.chaimen666.com/api VITE_DEFAULT_TENANT_CODE=qiwai-showcase npm --prefix apps/mobile run build:mp-weixin` | 通过 |
| `npm run test:preflight-guards` | 通过 |
| `npm run preflight` | 通过；仍提示生产短信 env 凭据为空，需后台或生产环境补齐 |
| `npm --prefix apps/api run migration:show` | 通过，本地库显示 `OperationSettingSmsSdkAppId1782880000000` 已执行 |
| `git diff --check` | 通过，仅 Windows LF/CRLF 提示 |
| `API_READY_URL=https://rd.chaimen666.com/api/health/ready npm run wait:api-ready` | 通过，线上 ready=true |

## 4. 线上探测结果

| 检查项 | 结果 |
| --- | --- |
| `GET /api/health/ready` | 200，`ready=true / api=up / database=up / config=warning` |
| 线上 API commit | `9260d75`，不是本地 `a6fffcb1` |
| `GET /version.json` | 返回 H5 HTML，不是 JSON 版本文件 |
| `GET /admin/version.json` | 返回 Admin HTML 片段，不是 JSON 版本文件 |
| `GET /api/public/marketing-popups?...pageKey=home&platform=h5` | 返回 `null` |
| `GET /api/public/ad-slots?...slotKey=home_top_banner` | 返回旧结构，`imageUrl=null`，未见 `resolvedImageUrl` |
| H5 首页/Admin 首页 HEAD | 200 |

结论：线上服务可用，但仍是旧发布状态；本轮代码未发布到服务器静态目录/API 进程。

## 5. 阻塞项

- 缺少服务器执行权限：本机没有远程发布脚本或 SSH/PM2 会话，不能执行生产备份、migration、API 重启、静态包发布和 Nginx reload。
- 缺少真实短信配置：`deploy/.env.production` 中 `SMS_ACCESS_KEY_ID`、`SMS_ACCESS_KEY_SECRET`、`SMS_SIGN_NAME`、`SMS_TEMPLATE_ID`、`SMS_SDK_APP_ID` 仍为空；可改为在后台系统设置中配置。
- 缺少演示账号密码：`scripts/verify-online-showcase-roles.mjs` 已验证入口可运行，但当前环境缺少 `SHOWCASE_PASSWORD`，无法登录复验店铺/代理/店铺财务角色。
- 缺少小程序体验版环境：本机已生成 mp-weixin 构建包，但未在微信开发者工具或体验版中完成登录、装修、弹窗、广告和跳转限制验证。

## 6. 服务器继续执行清单

1. 在服务器 `/www/wwwroot/rd.chaimen666.com` 拉取本轮代码。
2. 执行 `npm run db:backup`。
3. 执行 `npm --prefix apps/api run migration:show`，确认仅包含预期 migration；再执行 `npm --prefix apps/api run migration:run`。
4. 执行 `npm --prefix apps/api run build`，重启 `activity-api`。
5. 执行 `npm --prefix apps/admin run build`、`npm --prefix apps/mobile run build:h5`。
6. 执行小程序构建：`VITE_API_BASE=https://rd.chaimen666.com/api VITE_DEFAULT_TENANT_CODE=qiwai-showcase npm --prefix apps/mobile run build:mp-weixin`。
7. 执行 `WEBROOT=/www/wwwroot/rd.chaimen666.com/apps/mobile/dist/build/h5 ADMIN_WEBROOT=/www/wwwroot/rd.chaimen666.com/apps/admin/dist npm run publish:webroot`。
8. reload Nginx，并执行 `API_READY_URL=https://rd.chaimen666.com/api/health/ready npm run wait:api-ready`。
9. 登录后台配置腾讯云短信并执行测试短信。
10. 使用新手机号完成 H5 验证码登录、报名、财务确认、签到核销、会员统计闭环。
11. 设置 `SHOWCASE_PASSWORD` 后执行 `API_BASE=https://rd.chaimen666.com/api node scripts/verify-online-showcase-roles.mjs`。
12. 用微信开发者工具导入 `apps/mobile/dist/build/mp-weixin` 并完成体验版验证。
