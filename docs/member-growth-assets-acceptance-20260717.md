# 会员成长与资产验收记录（2026-07-17）

- 租户 tenantId=23，作用域 `tenant:23`；PC 账号 `showcase_ops`，H5 会员 `13990000005`。
- 会员档案共 387 条且 user+tenantScopeKey 无重复：平台 213、tenant:23 168、tenant:42 4、tenant:1 2。
- 双档案用户 192 的 tenant:23 流水 712 增加 3 分后按租户档案响应；设置过期并扫描后，租户积分回到 0，平台积分保持 20。
- 修复积分响应、标签筛选、档案并发创建、列表逐用户重算超时，以及 `.where()` 覆盖租户条件造成的跨租户列表越权。
- 排障期间旧请求误生成的 9,992 条 tenant:23 档案已按明确创建时间删除，恢复为 168 条；原有流水 712 和业务数据保留。
- 租户会员列表返回 168 条且全部为 `tenant:23`，首屏约 40ms。
- PC 显示总会员 168、已绑手机号 168、近 7 日活跃 132 和完整会员资产列；H5 显示当前租户等级、积分、成长、余额 6500.00、订单和下一等级差值；均无横向溢出。
- API 全量测试通过；API、PC、H5、微信小程序构建通过；API 容器 healthy。
- 正式微信真机仍需生产 AppID、合法域名和微信登录环境复核，不以本地 H5 代替真机结论。
