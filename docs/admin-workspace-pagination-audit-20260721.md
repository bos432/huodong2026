# 后台工作区分页分类与视觉巡检（2026-07-21）

## 1. 用户问题

- 公益概览、公益配置、公益项目、公益流水全部纵向堆叠，页面过长且难以定位。
- 公益贡献凭证底部日期、订单号和凭证编号发生文字重叠。
- 需要检查其他后台页面是否存在同类的长列表、无分页、无分类或文字溢出问题。

## 2. 公益后台整改

- 拆分为“公益概览 / 公益配置 / 公益项目 / 公益流水”四个标签工作区。
- 公益项目支持名称/编号、状态筛选，默认每页 10 条。
- 公益流水改为服务端分页，支持关键词、流水类型、来源筛选，默认每页 20 条，可完整查询 155 条现有流水。
- 旧版不带分页参数的接口继续返回数组，现有验收脚本保持兼容。
- 公益概览实测页面高度由约 8,900px 降为 1,227px；项目页显示 10 条，流水页显示 20 条。

## 3. 凭证布局整改

- 日期、订单号调整到同一行左右两侧，凭证编号独占下一行居中显示。
- 持有人、项目名称、订单号、凭证编号加入 Unicode 安全截断，防止异常长文本越界。
- “已冲正”水印降低透明度并调整位置，避免影响持有人可读性。
- 真实冲正凭证 `MPCG20260718-000152-7F0EF82D` 的全部可见文本矩形碰撞数为 0。

## 4. 全后台巡检与追加整改

- 浏览器逐个检查 62 个后台路由，页面级横向溢出 0。
- 源码扫描全部后台 Vue 视图的表格、分页、标签、客户端切片与服务端分页参数。
- 志愿者档案发现同类问题：原页面高 9,835px。档案、服务记录、任务报名分别增加独立分页，默认每页 10 条；整改后 2,017px。
- 系统设置已有四个一级标签，但运营设置仍堆叠。新增“报名与功能 / 支付与客服 / 短信服务 / 品牌主题 / 协议隐私”五个二级分段；整改后只渲染当前分组，桌面高度降为 3,176px。
- 管理员列表已有服务端分页，将默认页长从 20 调整为 10。
- 小程序发布、操作流程属于固定流程或说明内容；商城订单按业务模式展示且已有数据边界，不按无限增长列表处理。

## 5. 浏览器验收

- 桌面：公益、志愿者、系统设置页面宽度与视口一致，控制台 warning/error 0。
- 390px：公益、志愿者、系统设置的页面滚动宽度均为 375/375，无横向溢出。
- 公益项目分页 10 条；公益流水分页 20 条、总数 155。
- 志愿者三个标签总数分别为 93、48、54，当前档案页仅渲染 10 条。
- 系统设置二级分段 5 项完整，默认分组仅显示 6 个表单项。

## 6. 自动化验证

- 公益/分页/凭证专项：2 个测试文件、13 项通过。
- API 全量：174 个测试文件、992 项通过。
- API 与后台 production build 通过。
- `npm run monitor:health`：`status=ok`、`alerts=0`。
- `git diff --check`：退出 0，仅有既有换行符提示。

## 7. 已知外部边界

本地静态发布仍提示 API `BUILD_COMMIT` 元数据为空，这是本地 Docker 配置的已知警告，不影响本次分页、分类和布局功能。正式生产发布时按发布清单注入版本元数据。
