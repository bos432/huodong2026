# 候补名单状态闭环验收（2026-07-21）

## 范围

- 页面：`/admin/waitlists`
- 验收账号：`showcase_admin / Qiwai123456`
- 补齐活动选项和候补列表的失败清空、请求代次、筛选快照、响应校验与独立重试。
- 补齐补位和取消的目标、状态、筛选及列表代次保护；保留既有三权分离、敏感字段脱敏和服务端并发锁。
- 本轮只打开取消确认并返回，未执行补位、取消或其他业务写入。

## 实现

- 活动选项和候补列表使用独立 loading、持久错误和请求代次；请求开始、结构异常或失败时分别清空旧选项、旧行和 total。
- 列表绑定活动、状态、页码及页大小快照，迟到响应不能覆盖当前范围；首次加载通过 `Promise.allSettled` 并行读取两区。
- 补位和取消在确认框打开前锁定候补 ID，并冻结当前状态、筛选和列表代次；确认后再次复核目标仍在当前列表且仍为候补中。
- 读取、确认或写入期间锁定筛选、重试、分页和全部行操作；取消原因继续强制非空。

## 浏览器验收

- 正常态：1 条候补记录，记录 `#11`，活动 `候补权限验收-waitlist-permission-1784371870367`，敏感字段保持脱敏。
- 打开取消候补提示后，筛选、分页和行操作均锁定；点击“返回”后记录仍为候补中，未产生写入。
- 停止 `activity-api` 并重载后，旧行和 `Total 1` 立即清空；Nginx 超时后活动选项和候补列表分别持久显示 HTTP 502 及独立重试入口。
- API 恢复 healthy 后分别点击“重试加载活动”和“重试候补列表”，1 条候补记录及 `Total 1` 完整恢复。
- 390x844 下 `clientWidth=scrollWidth=390`，标题完整可见，控制台 warning/error 0。
- 截图：`.local-logs/waitlist-state-20260721/mobile.png`。
- 最终恢复 `showcase_admin` 工作台，桌面 `1066/1066`，控制台 warning/error 0。

## 自动化与运行态

- PC 生产构建通过，耗时 39.7 秒；并行包装进程在构建成功后触发 Windows Node `UV_HANDLE_CLOSING` 断言，串行复跑确认并非源码或构建失败。
- 串行专项 3 文件、38 项通过。
- 完整 `ci:verify` 130.8 秒退出 0；运行依赖 high/critical 0，全部测试、preflight 和 Shared/API/PC/H5/微信小程序构建通过。
- 监控 `status=ok`、`alerts=0`；readiness `ready=true`、`blockingCount=0`。
- 统一资金 `healthy=true`、`issueCount=0`；活动订单 539、课程订单 93、商城订单 815、钱包流水 471、商城结算 26。
- API/MySQL healthy，Nginx running；完整 `git diff --check` 退出 0，仅有既有 LF/CRLF 提示。

## 结论

计划表 `11.01.79` 候补名单状态闭环通过。既有计划 `11.01.35` 的权限账号、活动、候补、并发结果和审计数据全部保留。
