# 统一资金风险最终归档验收报告

验收日期：2026-07-20  
计划项：`03.06`、`11.01.54`  
结论：本地资金风险扫描、处置和积压归档通过；生产外部告警接收通道随 `12.03` 复核。

## 一、功能补齐

- 课程支付流水纳入 `payment_mismatch` 和 `duplicate_payment` 扫描。
- 课程退款失败纳入 `refund_failed`，通过课程订单和课程租户执行隔离。
- 统一扫描现覆盖活动、课程、商城、渠道账单、退款和钱包四资金桶。
- 不可变回调失败按回调日志 ID 表示一次独立事件，人工带依据解决后保持解决；新失败回调使用新 ID 和新指纹生成新告警。
- 负余额、支付差异、重复支付、退款失败等可变来源若仍存在，已解决告警仍会重新打开。
- 扫描结果拆分为 `observedCount`、`detectedCount`、`ignoredResolvedCount` 和 `openCount`，避免历史日志被误报成新故障。

## 二、课程真实受控演练

证据：`.local-logs/course-fund-risk-1784523215698/result.json`。

保留数据：

| 数据 | 标识 |
| --- | --- |
| 课程订单 | `#100 / CFRISK1784523215698` |
| 课程退款 | `#12 / CFRF1784523215698`，最终 rejected |
| 待对账支付流水 | `#236`，最终 matched |
| 课程风险告警 | `#34` 支付差异、`#35` 退款失败、`#36` 重复支付 |

验收过程：

- 临时生成一条课程支付待对账、两条同订单成功支付和一条失败退款。
- 扫描准确生成三类租户 `#23` 告警，业务类型分别为 `course_payment`、`course_refund`。
- 将支付流水恢复匹配，第二条受控支付重分类为 `statement_matched`，失败退款改为 rejected，不执行真实退款。
- 三条告警填写处理依据并解决；再次扫描保持 resolved。
- 最终同课程订单成功支付流水 1 条、待对账 0 条、失败退款 0 条。

## 三、历史积压整改

证据：`.local-logs/fund-risk-backlog-1784523429888/result.json`。

- 复核 24 条活动回调和 1 条商城回调，全部为历史签名失败、金额不一致或 `03.06` 受控失败验收；回调均已被拒绝，未改变订单。
- 25 条不可变回调告警逐条填写依据后解决，日志继续保留。
- 商城订单 `#948` 两条受控成功支付中，`RISK_MALL_PAY_B_1784223000000` 重分类为 `statement_matched`，最终成功支付仅 1 条。
- 商城退款 `RISK_MALL_RF_1784223000000` 从受控 failed 恢复为 rejected，失败原因清空，不执行真实退款。
- 对应商城重复支付和退款失败告警带依据解决。
- 最终开放告警 0、跟进中告警 0。

## 四、浏览器验收

- 平台管理员打开 PC 全局对账，慢π演示中心“异常提醒”为 0，待处理告警为空。
- 切换“已解决”后可见课程支付差异、课程重复支付、课程退款失败三条记录，均显示业务编号、发现次数、处理人和处理依据。
- 告警工作台容器 `scrollWidth=669`、`clientWidth=669`，无横向溢出。
- 控制台 warning/error 为 0。
- 截图：`.local-logs/course-fund-risk-1784523215698/pc-finance-resolved.png`。

## 五、最终门禁

| 检查 | 结果 |
| --- | --- |
| 资金风险专项 | 3 文件、7 项通过 |
| API 全量测试 | 152 文件、862 项通过 |
| API 构建 / Docker 重建 | 通过，API healthy |
| 完整 preflight | 通过；仅正式短信生产凭据提示 |
| readiness | `ready=true`、`blockingCount=0` |
| 统一资金一致性 | `healthy=true`、`issueCount=0` |
| 最终风险扫描 | `observed=25`、`detected=0`、`ignoredResolved=25`、`open=0` |
| 差异检查 | 通过，仅 Windows 行尾提示 |

## 六、生产复核

- 外部监控告警接收地址、通知值班人、升级策略和故障演练由 `12.03` 在生产环境执行。
- 正式微信/支付宝渠道故障仍需使用真实商户配置做回调失败、退款失败和恢复演练。
