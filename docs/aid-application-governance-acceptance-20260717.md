# 10.02 援助申请与敏感材料治理验收报告

## 验收结论

- 援助申请并发提交、加密落盘、材料魔数校验、补件、职责分离、敏感查看审计、私有备份恢复和 PC/H5 流程已通过。
- 姓名、身份证、地址、情况说明、补件、审核意见和原文件名均未以明文落库。
- 微信小程序真机文件选择、上传和下载仍按生产环境终验。

## 保留数据

- 申请：`11 / AID20260717563BA9954B`
- 材料：`11`
- 申请会员：`13994664467 / Qiwai123456`
- 跟进管理员：`acceptance_platform_manager / 165`
- 复核管理员：`acceptance_platform_reviewer / 166`
- 敏感查看管理员：`acceptance_platform_sensitive / 167`
- 只读管理员：`acceptance_platform_viewer / 168`

## 并发与权限验收

```powershell
npm run acceptance:premium-roles
```

- 同业务键两路并发提交只生成申请 11。
- 同业务键两路并发补件只推进一次状态和事件。
- 同业务键两路并发最终审批均返回申请 11 的 approved 状态。
- 跟进人不能执行最终审批，只读账号不能查看敏感明文。
- 只有 `aid.sensitive` 账号可以解密查看，且查看动作写入审计事件。
- 伪造 PDF MIME 的文件返回 400，真实 PDF 魔数文件加密保存。

## 加密落盘

- `sensitivePayloadEncrypted` 长度 530，前缀为 `enc:v1:`。
- `supplementRequestEncrypted` 和 `reviewRemarkEncrypted` 均为密文。
- 姓名、身份证和补件明文在密文字段中的命中数均为 0。
- 手机只保存 64 位 HMAC 盲索引和 `139****4467` 脱敏值。
- 原文件名字段前缀为 `enc:v1:`，明文文件名命中数为 0。
- 私有引用为 `secure-aid-document://...pdf.enc`，不映射到 Nginx 公共目录。

## 备份恢复

- 备份：`backups/private-data/private-data-20260717-203613.tar.gz`，约 20.18 MB。
- 隔离恢复目录：`backups/aid-restore-20260717-203616`。
- 援助材料原卷与恢复副本 SHA-256 均为 `4c6780eb2203b2325177f2569c89f8c1c5d89a0167aa25dbb303aa3eba6600c7`。
- 恢复脚本已执行路径穿越检查和显式确认。

## 浏览器验收

### PC

- 列表仅显示脱敏姓名、手机号、类型、城市、帮扶方向、状态、材料数和负责人。
- 申请 11 显示已批准、1 份材料和独立跟进人。
- 页面无横向溢出，控制台 warning/error 为 0。

### H5

- 临时增值权益窗口内，会员 `13994664467` 可打开援助申请页。
- 我的申请显示申请编号、已批准、帮扶方向、材料数量和审核意见。
- 验收后租户套餐与权益已恢复原配置。
- 页面无横向溢出，控制台 warning/error 为 0。

## 全量回归

- API：115 个测试文件、629 项测试通过。
- shared/API/PC/H5/微信小程序构建通过。
- `git diff --check` 通过。

## 待生产终验

- 微信开发者工具和真机验证文件选择、相机/相册权限、补件上传及私有下载。
- 生产对象存储或私有卷执行异地备份、恢复和保留期演练。
- 对上传、盲索引检索、敏感查看和材料下载执行外部渗透测试。
