# 首页装修事务替换与错误恢复验收

## 完成范围

- 新增 `POST /admin/homepage/sections/replace`，整页模块删除与重建在同一数据库事务内完成。
- 内置模板、上线简洁版、UI 整套模板、视觉风格、同商家页面复制、跨商家复制和本地发布快照恢复统一使用事务接口。
- 恢复默认装修改为复用同一事务能力，不再先删除后调用独立创建流程。
- 支持合法空页面快照，整页清空仍在事务内执行。
- DTO 限制每次最多 100 个模块，逐项校验类型、状态、排序、配置和布局。
- PC 页面使用 `homepage.manage` 二次判权，补主加载、商家列表、版本历史和模板库持久错误及独立重试。
- 发布、应用模板、复制页面、恢复发布版本和恢复默认装修增加主操作互斥；模块复制、启停、删除和排序增加行级互斥。
- 所有确认框取消统一吞掉预期取消事件，恢复按钮和筛选控件，不产生未处理 Promise。

## 自动化与数据库验收

- 专项：4 个文件、65 项通过。
- API 全量：119 个文件、664 项通过。
- API 与 PC 生产构建通过，PC 1945 个模块。
- 完整 `npm run preflight` 通过，权限目录 87 项。
- 真实结果：`.local-logs/homepage-replace-1784342241424/result.json`。

真实 MySQL 使用原本为空的 `login_page` 验收：

1. 事务替换成功创建 2 个模块，排序规范化为 10、20。
2. 第二次替换使用 121 字符标题触发数据库列写入失败。
3. 失败后原 2 个模块数量、标题和业务标记全部保持不变，证明删除与重建完整回滚。
4. 使用空数组事务化恢复空页面，最终 `login_page` 模块数重新为 0。

验收标记 `事务替换验收-1784342241660` 已清理，未改变首页现有 11 个模块、发布快照和历史版本。

## 浏览器验收

- 账号：`showcase_admin / Qiwai123456`。
- `/admin/homepage-builder?pageKey=home` 真实加载首页 11 个模块和最近发布信息。
- “恢复默认装修”确认框选择取消后，首页仍为 11 个模块，首模块仍为“慢π演示中心”，操作按钮恢复可用。
- 版本历史真实加载 3 个保留版本，无错误空态。
- 模板库真实加载，当前无保存模板时显示正确空态，不显示接口失败为假空数据。
- 桌面 `scrollWidth=clientWidth=1164`，390×844 下 `scrollWidth=clientWidth=375`。
- 页面刷新后新增 console warning/error 为 0。

## 外部边界

本批不发布草稿、不覆盖正式发布快照，也不新增长期装修模块。H5 与微信小程序正式发布后的 CDN/微信缓存刷新继续在生产域名和正式小程序环境复核。
