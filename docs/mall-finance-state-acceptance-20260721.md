# 商城结算、财务与经营统计状态闭环验收报告

验收日期：2026-07-21  
工作包：`11.01.62`  
验收结论：通过。本批未生成、审核、拒绝、打款、扣回或调整结算单，现有测试数据全部保留。

## 一、实现范围

### 商城结算

- 结算列表增加租户、店铺、筛选上下文和请求代次，刷新开始清空旧结算、待生成数据及汇总。
- 列表刷新或范围切换会关闭并废弃旧明细抽屉；明细响应复核结算单和店铺归属。
- 生成结算单补二次确认，并冻结店铺、周期和待生成列表代次。
- 审核、拒绝、打款/扣回在确认后复核结算单号、店铺、筛选和列表代次。
- 财务调整绑定明细抽屉的结算单和请求代次，目标变化时拒绝提交。

### 商城财务总览

- 订单摘要、支付流水、退款日志、佣金摘要和结算风险拆为五个独立分区。
- 每个分区独立维护请求代次、加载态、旧数据清理、错误正文和重试入口。
- 五分区使用 `Promise.allSettled` 并行加载，单个接口失败不再拖垮其它财务数据。
- 导出期间锁定租户、店铺、支付方式、日期、关键词和结算组筛选。

### 商城经营统计

- 租户、店铺和统计聚合分别增加请求代次及上下文复核。
- 刷新开始立即清空旧指标和六张分析表；失败正文显式展示并可独立重试。
- 范围失败同步清除旧经营数据，迟到响应不能写回新店铺。

## 二、浏览器正常态

店铺：`#38 / 慢π自营店`。

| 页面 | 真实读取结果 |
| --- | --- |
| 财务总览 | 订单 265、实收 10993.77 元、净收 9957.02 元、已退 1036.75 元、待处理售后 21 |
| 财务分区 | 支付流水 48、退款日志 18、结算风险 1、佣金总额 37.10 元 |
| 经营统计 | 30 天订单 193、实收 10993.77 元、净收 9957.02 元、优惠 362.25 元、已退 1036.75 元 |
| 统计明细 | 趋势 7、支付方式 3、订单状态 6、售后状态 4、热销商品 2、优惠券转化 3 |
| 商城结算 | 待生成店铺 1、结算单 1、净交易额 -59.00 元、应扣回 59.00 元 |

平台管理员打开结算单 `MS202607172010495660`，逐笔明细金额一致，显示退款明细 1 行、状态事件 1 条。运营账号读取同一明细返回 403，符合逐笔结算权限边界。

平台管理员打开审核确认框后，租户、店铺、状态、日期、刷新、导出和所有行操作全部锁定；取消后结算单继续保持草稿。

## 三、故障与恢复

受控停止 `activity-api`，未停止 MySQL、未执行 migration、未修改资金或业务数据：

- 财务八项汇总全部归零，支付、退款和结算表清空。
- 订单摘要、支付流水、退款日志、佣金摘要和结算风险分别显示 HTTP 502 正文及独立重试。
- 经营统计五项指标与六张分析表全部归零，统计错误正文及重试入口可见。
- 结算六项汇总、待生成列表和结算单列表全部归零，旧明细被关闭，结算错误正文可见。
- API 每次恢复后均达到 healthy，三个页面真实数量完整恢复。

## 四、移动端验收

390x844 视口下三个页面实际内容宽度均为 375px：

- 财务、统计、结算页面均 `clientWidth=scrollWidth=375`。
- 页面内容区均为 351/351，标题均为 323x31px。
- 租户/店铺筛选、快捷操作和首屏统计无重叠，无页面级横向溢出。

截图：

- `.local-logs/mall-finance-state-20260721/mobile-mall-finance-390x844.png`
- `.local-logs/mall-finance-state-20260721/mobile-mall-statistics-390x844.png`
- `.local-logs/mall-finance-state-20260721/mobile-mall-settlements-390x844.png`

## 五、自动化与运行态

| 检查 | 结果 |
| --- | --- |
| `menu-integrity.spec.ts` | 29/29，通过结算目标、财务五分区和统计竞态合同 |
| API 全量测试 | 158 个文件、915 项全部通过 |
| `npm run ci:verify` | 130.1 秒退出 0 |
| 全端构建 | Shared、API、PC、H5、微信小程序全部通过 |
| 运行时依赖审计 | high=0、critical=0 |
| `npm run monitor:health` | `status=ok`、`alerts=0` |
| 统一资金一致性 | `healthy=true`、`issueCount=0`；活动 539、课程 93、商城 815、钱包流水 471、结算 26 |
| `/api/health/ready` | `ready=true`、`blockingCount=0` |
| `git diff --check` | 退出 0，仅有工作区既有换行提示 |

## 六、结论

商城结算、财务总览和经营统计的读取真实性、范围竞态、明细目标、资金操作确认、错误恢复和移动布局已经闭环。下一工作包继续 `11.01` 其余 PC 长尾页面。
