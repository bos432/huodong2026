# 前台装修工作台实时预览重构验收（2026-07-21）

## 交付范围

- 将原四栏、按钮密集的装修页重构为“页面结构 / 实时前端预览 / 模块属性”三栏工作台。
- 页面和商家范围、线上 H5、生效检测、保存版本、发布草稿保留在顶部主工具栏。
- 模板、历史、页面复制、跨商家复制、恢复线上、恢复默认和教程收纳到“更多”菜单。
- 添加模块改为支持搜索和基础、内容、业务、页面分类的模块库。
- 新增 `HomepageLivePreview.vue`，覆盖搜索、主视觉、公告、快捷入口、Banner、富文本、分类、活动、心得、品牌、公益、专题、商城、我的页、内页和底部导航。
- 预览直接合并当前未保存表单，标题、显示状态、布局和样式无需先保存即可更新；保存和发布边界不变。
- 375 / 430 设备外框使用 357+18 / 412+18 的稳定尺寸，窄屏自动缩放到容器宽度。

## 兼容边界

- 未修改 `/admin/homepage/sections/replace`、草稿、发布快照、版本历史、模板和跨租户复制协议。
- 未清理数据库、测试数据、上传文件、备份或既有发布历史。
- 浏览器验收只修改本地未保存表单并恢复原值，没有保存或发布测试装修。
- 现有底部导航隐藏/发布逻辑继续由原数据与公开装修规则控制。

## 自动化验证

- 装修与菜单专项：2 文件 37 项通过。
- 无障碍回归与装修合同：2 文件 9 项通过。
- API 全量：175 文件、998 项通过。
- `npm run build:api`：通过。
- `npm run build:admin`：通过。
- `npm run build:mobile:h5`：通过。
- `npm run build:mobile:mp-weixin`：通过。
- `npm run security:secrets`：1,356 个文件通过。
- `npm run monitor:health`：`status=ok`、`alerts=0`。
- `git diff --check`：通过，仅有既有换行符提示。

## 浏览器融合验收

- 环境：`http://127.0.0.1:18080/admin/homepage-builder`，平台超管登录态，既有 MySQL 数据卷。
- 桌面 1066px：工作台按两列显示结构和预览，属性面板进入下一行；页面 `scrollWidth=clientWidth=1066`。
- 移动 390x844 覆盖：三块单列排列；页面 `scrollWidth=clientWidth=375`，属性面板内部 `scrollWidth=clientWidth=325`。
- 当前 22 个装修模块对应 22 个真实预览块；活动和心得使用接近 H5 的卡片、列表和内容结构。
- 点击手机预览模块后，对应模块高亮并打开属性面板。
- 标题临时改为“实时预览融合验收”后，未保存预览命中 1 个；恢复原值并刷新后测试标题不存在，数据库原标题仍在。
- 模块库加载 20 种模块；搜索“公益”仅显示“公益公示摘要”，分类切换可用。
- 375 外框宽 375px，430 外框宽 430px；切换不引起页面宽度变化。
- “更多”菜单包含模板、历史、复制、恢复和教程入口。
- 桌面、移动端控制台 warning/error 均为 0。

## 整改记录

- 首次全量测试发现模块行纯图标操作按钮缺少 `title`；补齐后无障碍合同和 998 项全量测试通过。
- 首次窄屏截图发现模块类型标签被长标题挤成竖排；模块操作调整到第二行后，标题区域宽度由 57px 提升至 167px。
- 首次移动截图发现属性面板因 12px 内边距与 `-16px` 吸顶外边距不一致而多出 4px 横向滚动；统一移动端边距后面板宽度完全一致。

## 结论

装修工作台重构、本地融合测试和交付验证通过，可进入 r43 候选组装。生产上线仍应按既有发布流程先备份、保存草稿、预览，再由授权管理员发布。
