# 商家资料状态闭环验收（2026-07-21）

## 范围

- 页面：`/admin/tenant-profile`
- 验收账号：`showcase_admin / Qiwai123456`
- 补齐商家资料读取的失败清空、请求代次和响应校验。
- 补齐保存确认阶段的表单快照、商家 ID/编码和读取代次保护。
- 保留既有 `tenant_profile.manage` 最小权限、输入边界、恢复原值、租户边界和更新审计。
- 本轮只在前端临时修改联系人，打开保存确认后取消并恢复原值，未执行资料写入。

## 实现

- 商家资料每次读取增加请求代次；请求开始、响应结构异常或失败时清空旧 profile、表单、已保存快照和保存错误。
- 响应必须包含有效 ID、非空编码、名称和布尔启停状态才允许渲染；故障时资料头和表单不挂载，不再把空对象误显示成“已停用”。
- 保存前冻结规范化表单、商家 ID、编码和当前读取代次；确认后再次复核全部上下文。
- 保存确认框出现前设置 `confirming`，读取、确认或保存期间锁定刷新、四个字段、恢复原值和保存按钮。
- 保存响应再次校验结构、商家 ID 和编码后才回填本地资料及商家名称缓存。
- 菜单完整性合同同步升级，继续固定取消确认可靠返回，并要求释放 confirming 锁和统一 scope 锁。

## 浏览器验收

- 正常态：名称 `慢π演示中心`、编码 `qiwai-showcase`、地区 `演示城市`、联系人 `慢π演示运营`、电话 `13990009999`，状态启用中。
- 将联系人临时改为“慢π演示运营（临时确认验收）”并打开保存确认后，刷新、四个字段、恢复原值和保存全部锁定。
- 点击“再检查一下”后确认框关闭，再点击“恢复原值”，联系人恢复为 `慢π演示运营`，保存按钮重新禁用，未产生写入。
- 停止 `activity-api` 并重载后，资料头和表单立即清空；Nginx 超时后持久显示 HTTP 502 和“重新加载”，页面未残留名称、联系人或伪启停状态。
- API 恢复 healthy 后点击“重新加载”，名称、编码、地区、联系人和电话完整恢复。
- 390x844 下 `clientWidth=scrollWidth=375`，业务卡片 `325/325`，控制台 warning/error 0。
- 截图：`.local-logs/tenant-profile-state-20260721/mobile.png`。
- 最终恢复 `showcase_admin` 工作台，桌面文档无横向溢出，控制台 warning/error 0。

## 自动化与运行态

- PC `vue-tsc` 和生产构建通过，1947 个模块，耗时 40.1 秒。
- 专项 2 文件、35 项通过；首轮发现菜单合同仍匹配旧取消与禁用表达式，升级合同后复跑通过。
- 完整 `ci:verify` 131.7 秒退出 0；运行依赖 high/critical 0，全部测试、preflight 和 Shared/API/PC/H5/微信小程序构建通过。
- 监控 `status=ok`、`alerts=0`；readiness `ready=true`、`blockingCount=0`。
- 统一资金 `healthy=true`、`issueCount=0`；活动订单 539、课程订单 93、商城订单 815、钱包流水 471、商城结算 26。
- API/MySQL healthy，Nginx running；完整 `git diff --check` 退出 0，仅有既有 LF/CRLF 提示。

## 结论

计划表 `11.01.82` 商家资料状态闭环通过。既有计划 `11.01.20` 的最小权限账号、资料内容和审计数据全部保留。
