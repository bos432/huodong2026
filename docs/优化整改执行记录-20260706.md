# 优化整改执行记录 2026-07-06

## 1. 结论

本轮发现的可执行整改已完成并复测通过。当前没有阻塞交付的代码问题。

## 2. 已执行整改

| 编号 | 问题 | 整改动作 | 验收结果 |
|---|---|---|---|
| R-01 | 运营巡检、角色权限、操作审计 guard 仍扫描旧的 `Layout.vue` 菜单结构。 | guard 改为同时扫描 `apps/admin/src/navigation/admin-menu.ts`，适配后台菜单抽离后的结构。 | `npm run preflight` 通过 |
| R-02 | 后台菜单分组文案未完全贴合成熟运营口径。 | 平台“系统安全”改为“系统与安全”；商家“订单财务”改为“票务与财务”。 | 后台构建通过，菜单权限测试通过 |
| R-03 | 本机 Node v25 会导致 uni-app 小程序构建进程崩溃。 | `scripts/codex-audit.mjs` 自动发现兼容 Node 24，并在 release-check 中用 Node 24 构建 mp-weixin。 | 小程序构建通过 |
| R-04 | H5 浏览器验收误点页面装修中的“登录”文案，未点击真实提交按钮。 | 验收脚本改为定位 `.phone-login-section .button`。 | H5 登录、报名、签到码、核销全流程通过 |
| R-05 | 手机管理端验收脚本只找原生 `button`，未兼容 uni-app `uni-button`。 | 验收脚本按钮选择器兼容 `uni-button.button.primary`。 | 手机管理端登录、发布活动、报名/订单/签到页通过 |

## 3. 验证命令

| 命令 | 结果 |
|---|---|
| `node scripts/preflight-ops-routine-guard.mjs` | 通过 |
| `node scripts/preflight-admin-role-guard.mjs` | 通过 |
| `node scripts/preflight-operation-audit-guard.mjs` | 通过 |
| `npm --prefix apps/api run test -- admin-roles.spec.ts admin-permissions.spec.ts menu-integrity.spec.ts` | 通过，21 个用例 |
| `npm --prefix apps/admin run build` | 通过 |
| `npm run build` | 通过 |
| `npm --prefix apps/mobile run build:mp-weixin` | 使用 Node 24 路径通过 |
| `npm run preflight` | 通过，仅短信凭证 warning |
| `npm run codex:release-check` | 通过，状态 warning 仅因本轮代码尚未提交 |

## 4. 剩余非代码项

| 项目 | 等级 | 说明 |
|---|---|---|
| 生产短信凭证 | 外部配置 | 真实短信服务商 Key、签名、模板需在生产后台配置并发送测试短信。 |
| 平台超管正式账号 | 外部账号 | 正式账号密码由甲方保存，本轮未明文写入文档。 |
| 小程序上传审核 | 人工发布 | 小程序包已构建，仍需微信开发者工具导入、上传体验版、提交审核。 |

## 5. 当前交付判断

可以进入部署和交付培训。部署后必须执行健康检查、版本检查、H5/后台关键流程和小程序体验版检查。
