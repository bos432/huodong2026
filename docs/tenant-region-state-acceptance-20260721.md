# 区域保护状态闭环验收（2026-07-21）

## 范围

- 页面：`/admin/tenant-regions`
- 平台验收账号：`admin`
- 补齐商家选项和区域列表的独立失败清空、请求代次、筛选快照、响应校验及重试。
- 加固新增/编辑、审批、删除和批量导入的目标与范围保护；保留既有权限分层、冲突规则、区域围栏和验收数据。

## 实现

- 商家选项和区域列表拆分为独立 loading、错误、请求代次和重试；顶部刷新使用 `Promise.allSettled` 并行刷新，商家筛选只刷新区域列表。
- 请求开始、响应异常或失败时仅清空对应旧数据；区域列表绑定商家筛选快照，迟到响应不能覆盖当前范围。
- 新增/编辑、审批、删除和批量导入冻结区域 ID、授权状态、商家筛选及列表代次，确认后再次复核当前目标。
- 编辑既有区域时商家归属只读，函数层再次拒绝迁移；读取、写入、编辑弹窗或导入弹窗期间锁定顶部范围及行操作。
- 保存或导入期间禁止通过遮罩、Escape 或关闭按钮退出弹窗。

## 浏览器验收

- 正常态：45 个商家选项、20 条区域、待审批 1 条；桌面 `1066/1066`，控制台 warning/error 0。
- 打开“演示城市核心区”编辑弹窗后，所属商家不可编辑，刷新、筛选和全部行操作锁定；点击取消后未写入数据。
- 打开待审批区域 `区域浏览器审批待处理-1784353998203` 的批准提示后，范围控件锁定；点击 `Cancel` 后该区域仍为待审批。
- 停止 `activity-api` 并重载后，商家选项和区域列表均清空，分别持久显示 HTTP 502 与独立重试入口。
- API 恢复 healthy 后先重试商家选项、再重试区域列表，恢复 45 个商家选项、20 条区域和 1 条待审批记录。
- 390x844 下 `clientWidth=scrollWidth=375`，标题完整可见，控制台 warning/error 0。
- 截图：`.local-logs/tenant-region-state-20260721/mobile.png`。
- 最终恢复 `showcase_admin` 工作台，桌面 `1066/1066`，控制台 warning/error 0。

## 自动化与运行态

- PC 类型检查和生产构建通过，耗时 40.6 秒。
- 专项 3 文件、12 项通过。
- 完整 `ci:verify` 132 秒退出 0；运行依赖 high/critical 0，全部测试、preflight 和 Shared/API/PC/H5/微信小程序构建通过。
- 监控 `status=ok`、`alerts=0`；readiness `ready=true`、`blockingCount=0`。
- 统一资金 `healthy=true`、`issueCount=0`；活动订单 539、课程订单 93、商城订单 815、钱包流水 471、商城结算 26。
- API/MySQL healthy，Nginx running；`git diff --check` 退出 0，仅有既有 LF/CRLF 提示。

## 结论

计划表 `11.01.78` 区域保护状态闭环通过。既有计划 `11.01.29` 的权限验收报告、账号、区域围栏、授权状态和审计记录全部保留。
