# 部署文档 2026-07-05

## 当前状态

- 线上 API 当前提交：`988f88f`
- 本地还有文档提交未推送：`f9229491`、`d1a0cdd2`
- 本次验收新文档待提交后也需要推送
- 本次主要是文档和验收，不包含新的功能代码修复；如只部署当前线上功能，不需要重新提交小程序审核。

## GitHub 推送

本机 GitHub 连接不稳定，优先用临时清空代理命令：

```powershell
git -C "E:\2027\活动报名\活动报名" -c http.proxy= -c https.proxy= -c http.version=HTTP/1.1 push origin feature/qiwai-ui-experiment
```

确认：

```powershell
git -C "E:\2027\活动报名\活动报名" status -sb
git -C "E:\2027\活动报名\活动报名" -c http.proxy= -c https.proxy= ls-remote origin refs/heads/feature/qiwai-ui-experiment
```

## 宝塔终端部署命令

如果后续需要把最新代码部署到线上，使用：

```bash
cd /www/wwwroot/rd.chaimen666.com
set -e

PM2=/www/server/nodejs/v22.22.3/lib/node_modules/pm2/bin/pm2

git fetch origin
git pull --ff-only origin feature/qiwai-ui-experiment

COMMIT=$(git rev-parse --short HEAD)
NOW=$(date -Iseconds)
sed -i "s|^BUILD_COMMIT=.*|BUILD_COMMIT=$COMMIT|" .env apps/api/.env
sed -i "s|^BUILD_TIME=.*|BUILD_TIME=$NOW|" .env apps/api/.env
export BUILD_COMMIT="$COMMIT"
export BUILD_TIME="$NOW"

npm --prefix apps/api run build
npm --prefix apps/admin run build

$PM2 restart activity-api --update-env
$PM2 save

/www/server/nginx/sbin/nginx -t
/www/server/nginx/sbin/nginx -s reload

curl -i --max-time 10 https://rd.chaimen666.com/api/health/ready
curl -i --max-time 10 https://rd.chaimen666.com/admin/version.json
```

## H5 和小程序构建

H5 构建：

```bash
export VITE_API_BASE=https://rd.chaimen666.com/api
export VITE_DEFAULT_TENANT_CODE=qiwai-showcase
npm --prefix apps/mobile run build:h5
```

小程序构建：

```bash
export VITE_API_BASE=https://rd.chaimen666.com/api
export VITE_DEFAULT_TENANT_CODE=qiwai-showcase
npm --prefix apps/mobile run build:mp-weixin
```

小程序构建产物：

```text
apps/mobile/dist/build/mp-weixin
```

## 是否需要小程序审核

- 只改 API、PC 后台、文档：不需要重新提交小程序审核。
- 改 `apps/mobile` 或重新发布小程序体验版：需要上传并提交微信审核。
- 本次验收本身未改小程序代码；但如果要把之前 `05a89bea` 的签到二维码展示修复上线到小程序，必须确认该提交对应的小程序包已上传审核并发布。

## 部署后检查

```bash
curl -i --max-time 10 https://rd.chaimen666.com/api/health/ready
curl -i --max-time 10 https://rd.chaimen666.com/admin/version.json
curl -i --max-time 10 https://rd.chaimen666.com/version.json
```

重点看：

- `ready=true`
- `database=up`
- `release.commit` 等于服务器当前 `git rev-parse --short HEAD`
- Admin/H5 `version.json` 提交号与部署版本一致

## 回滚

如果部署后出现异常：

```bash
cd /www/wwwroot/rd.chaimen666.com
PM2=/www/server/nodejs/v22.22.3/lib/node_modules/pm2/bin/pm2

git log --oneline -n 10
git checkout <上一个稳定提交>
npm --prefix apps/api run build
npm --prefix apps/admin run build
$PM2 restart activity-api --update-env
/www/server/nginx/sbin/nginx -t && /www/server/nginx/sbin/nginx -s reload
curl -i --max-time 10 https://rd.chaimen666.com/api/health/ready
```
