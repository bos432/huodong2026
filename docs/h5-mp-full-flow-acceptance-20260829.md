# H5 与微信小程序完整流程验收（2026-08-29）

## 验收对象

- 线上 H5：`https://rd.chaimen666.com/?tenantCode=cq_tongliang#/`
- 本地候选 H5：`http://127.0.0.1:5206/?tenantCode=cq_tongliang#/`
- 微信开发者工具项目：`apps/mobile/dist/build/mp-weixin`
- 微信模拟器：iPhone 12/13 (Pro)，390 x 844，基础库 3.16.2
- 线上 API/H5 版本：`bc7ea31d`
- 本地候选包标识：`working-tree`，禁止按旧提交号上传

## 已通过

### H5

- 首页加载、主推活动、日期活动流、真实名额与价格展示。
- 首页 -> 活动详情；详情时间、地点、名额、主办方、分享、客服和报名状态正确。
- 首页 -> 活动列表 -> 我的 -> 首页，底部导航路径正确且持续显示。
- 商家切换面板完整显示；铜梁与演示商家可以切换并恢复，租户参数同步更新。
- 登录入口、密码/验证码模式及空表单校验正常。
- 使用保留测试账号完成密码登录；登录后刷新仍保持会话，会员信息正常。
- 活动详情 -> 浏览足迹写入 -> 足迹返回详情正常；订单零数据空态正常，无持续加载。
- 订单页底部“我的”保持所属栏目选中态，点击后可正常返回个人中心根页。
- H5 扫码降级页可打开合法慢π活动链接，并拦截未知外部链接。
- 过审模式下社交拓展入口隐藏；直接访问受限路由会显示说明并安全返回首页。
- 375、390、760 像素下的首页、活动列表和活动详情无横向溢出、固定栏越界及控制台错误。

### 微信小程序

- 开发者工具自动化连接、重新编译和页面读取正常。
- 首页 -> 主推活动 -> 活动详情正常，未捕获 console exception。
- 活动列表筛选区、日期分组、名额和报名操作文案正常。
- 首页 -> 活动 -> 我的 -> 首页，三项底部导航可达。
- 已登录测试用户的我的、订单列表、报名详情、签到码和活动空间正常。
- 报名详情 -> 活动空间可查看公告空态、脱敏成员、问答空态、地点、签到和客服入口。
- 我的 -> 浏览足迹 -> 活动详情正常；历史活动保留在个人足迹中。
- 商家切换面板在 390 像素宽度内完整显示；自动化确认后可切换并恢复铜梁。
- 搜索页、扫码页和过审功能拦截页正常。
- 原 `activity-history` 404 未复现。

### 自动化与构建

- API 全量测试：191 个文件、1088 项测试全部通过。
- 社交资料、迁移和客户端状态契约：92 项全部通过。
- 上线前保护检查全部通过。
- shared、API、管理后台、H5、MP-Weixin 构建全部通过。
- MP-Weixin 产物依赖检查通过。

## 本轮修正

- 生产数据库补跑缺失迁移，修复活动详情的 `tenant_followers` 缺表错误。
- 放宽报名退款一致性契约对 uni-app 导入顺序的错误假设；仍严格验证 `onShow` 导入和前台刷新逻辑。
- 本地 H5 改用 Vite `/api` 代理连接线上 API，消除跨域导致的假失败。

## 上线阻断与未验证项

1. 线上管理后台仍为 `a8757abd`，API/H5 为 `bc7ea31d`。发布前必须统一三端提交号。
2. 本地小程序候选包包含未提交代码，版本文件为 `working-tree`。形成唯一 Git 提交并从该提交重新构建后才能上传。
3. 对象存储为 `before_launch` 警告，图片上传和持久化上线前必须配置并实测。
4. 短信、邮件、微信订阅消息、自动关单 Worker、通知调度 Worker 当前未启用，不能宣称真实通知和自动任务已验收。
5. 未执行真实微信支付、退款、短信发送、订阅消息、相机扫码和相册长按识别；这些必须由体验版真机完成。
6. 未创建新报名或触发退款，避免在未确认时改动生产业务数据；已使用保留测试报名验证报名后链路。

## 发布结论

当前线上核心活动浏览链路可用，本地候选版本的代码测试和构建通过，但仍是“待发布候选”，不是“可直接上传的正式包”。完成版本统一、对象存储配置、H5 登录态和真机外部能力验收后，方可进入正式发布。
