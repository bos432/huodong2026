# 移动资金异常状态与并发验收报告

日期：2026-07-20  
页面：`http://127.0.0.1:18080/h5/#/pages/admin/risk-alerts`

## 结论

本批通过。移动资金异常页已完成管理会话、租户、筛选、故障恢复、操作互斥和无障碍状态治理；服务端扫描与处置增加并发保护。移动客户端状态合同达到 71/71 页。

## 实现

- 列表请求绑定管理令牌、租户、角色、状态和类型；上下文变化立即清除旧告警，迟到响应、错误和 loading 不得写回新会话。
- bootstrap 与列表响应执行结构校验；主请求失败清除旧卡片，只显示一个持久错误和一个重试入口。
- 扫描和处置分别使用操作序号及会话复核；处置确认冻结告警 ID、原状态、标题和业务编号，确认后再次校验权限、会话及当前行状态。
- 页面错误与处置错误分层，处置失败不销毁已加载列表；所有时间按 `Asia/Shanghai` 展示。
- 状态、类型、刷新、扫描、重试和行操作补齐按钮或标签页语义、Enter、Space、忙碌及禁用状态。
- 服务端扫描使用 MySQL 命名锁串行化；告警处置在事务内悲观锁行，重复动作幂等返回 `operationApplied=false`，非法状态迁移和超过 500 字处理依据被拒绝。

## 浏览器验收

| 场景 | 结果 |
|---|---|
| 财务角色 | `showcase_finance` 可查看和处置，已解决共 12 条 |
| 键盘筛选 | Enter 选择“已解决”，Space 选择“回调”，结果 2 条 |
| 处置取消 | 打开告警 `#7` 重新打开弹窗后取消，状态保持 resolved，全部控件解锁 |
| 故障恢复 | 停止 API 后旧卡片从 2 条清为 0，仅显示一个重试；恢复 API 后回到 2 条 |
| 只读角色 | `showcase_agent_owner` 可查看 2 条回调告警，扫描按钮 0、处置按钮 0，扫描 API 返回 403 |
| 响应式 | 390x844 与 1280x800 均 `scrollWidth=clientWidth` |
| 控制台 | 本轮 warning/error 0 |

## 服务端并发

- 对告警 `#7` 并发发送两次 resolved -> open：两次 HTTP 201，仅一次 `operationApplied=true`，另一次为幂等 `false`。
- 随后恢复为 resolved，处理依据保留为 `mobile-risk-concurrency-20260720 并发状态机验收后恢复归档`。
- 非法状态筛选返回 400；只读角色扫描返回 403。
- 最终统一资金一致性 `healthy=true`、`issueCount=0`。

## 自动化与证据

- 风险状态专项：2 个测试文件、60 项通过。
- API 全量：158 个测试文件、907 项通过。
- API、H5、微信小程序构建通过。
- 完整 preflight 通过，仅保留正式短信凭据配置提醒。
- readiness：`ready=true`、`blockingCount=0`。
- Docker API/MySQL healthy，`git diff --check` 通过。
- 截图及结构化结果：`.local-logs/mobile-risk-state-20260720/`。

正式支付渠道失败回调、正式账单差异、微信真机长列表和弱网系统弹窗仍随生产配置与真机总验收复核。
