# 09.05 跨店拆单与统一支付验收报告

## 验收结论

跨店报价、整数分分摊、结算组、商户子订单、线下收款、余额统一支付和微信沙箱统一支付已完成真实 API/MySQL/H5 验收。正式微信支付与商户直收仍需正式商户号、证书、HTTPS 回调和微信真机环境完成生产终验。

## 本轮整改

- 修复跨店支付任务摘要未读取租户 `paymentMethods`、错误显示“商家未开启微信支付”的问题。
- 结算组任务现在与真实支付入口共用租户运营设置和平台运行配置；微信沙箱任务创建时 `paymentReady=true` 且无错误禁用原因。
- 微信沙箱仅在验收窗口临时启用，验收完成后平台 `paymentSandboxEnabled` 已恢复为 `false`，密钥字段恢复为空。

## 保留数据

| 支付方式 | 结算组 | 子订单 | 状态 |
| --- | --- | --- | --- |
| 线下收款 | `164 / MCG1784282628011DC3038` | `1002 / 1003` | 待确认收款 |
| 余额支付 | `165 / MCG1784282797442BED66B` | `1004 / 1005` | 已支付 |
| 微信沙箱 | `166 / MCG1784283252306F29B19` | `1006 / 1007` | 已支付 |
| 微信任务整改复测 | `167 / MCG1784283571987903AC8` | `1008 / 1009` | 已支付 |

## 分摊与拆单

- SKU 118、119 分属商户 99、100。
- 结算总额 `19.80`，两个商户各分摊 `9.90`。
- 商品、运费、优惠和应付金额的分摊整数分合计与结算组完全一致。
- 相同 `clientOrderKey` 重放返回原结算组，不重复创建子订单或锁库存。
- 既有 50 路并发创建 50 个结算组、100 个子订单的性能验收继续有效，p95 `8,818.50 ms`，无死锁和库存差异。

## 余额统一支付

- 结算组 165 一次性扣除 `19.80`，子订单 1004、1005 同步进入 `paid`。
- 钱包流水 432 仅一条，幂等键 `mall_checkout_balance_pay:165`。
- 现金余额从 `2042.00` 变为 `2022.20`，未按子订单重复扣款。
- 重放结算请求不新增钱包流水。

## 微信沙箱统一支付

- 结算组 166、167 均生成一次结算组支付参数。
- 首次回调将两个子订单同时更新为已支付。
- 相同回调重复提交返回幂等成功，不重复扣库存或记录重复业务事件。
- 聚合支付状态返回 `localStatus=paid`、状态文本“支付成功”。
- 整改复测中两个支付任务均为 `paymentReady=true`、`disabledReason=""`。

## 浏览器验收

- H5 商城订单列表按店铺分别显示微信子订单 1006、1007。
- 两单均显示 `¥9.90`、微信支付、待发货和对应履约店铺。
- 线下子订单 1002、1003 显示待确认收款和超时释放提示。
- 页面控制台 warning/error 为 0。

## 回归结果

- `acceptance:mall-checkout-group` 的线下、余额、微信沙箱三种模式通过。
- API 115 个测试文件、629 项测试通过。
- shared、API、PC、H5、微信小程序构建通过。
- Docker API 健康检查通过。

## 生产终验边界

正式平台代收与商户直收不得以沙箱结果代替。生产验收需配置正式 AppID、商户号、API v3 密钥、私钥、平台证书、HTTPS 支付与退款回调，完成小额支付、重复回调、退款、对账和微信真机留档。
