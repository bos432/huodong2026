# 收款方式状态闭环验收（2026-07-21）

## 范围

- 页面：`/admin/agents`
- 账号：`showcase_admin`，租户 `#23 / 慢π演示中心`
- 覆盖商家选项、代理列表、支付账户、编辑目标保护、失败恢复与移动布局。
- 故障演练只停止和恢复 `activity-api`，未修改或清理现有数据。

## 实现

- 商家选项、代理列表和支付账户拆为独立 loading、错误与重试分区。
- 三类读取在请求开始和失败时清空旧数据；代理与账户汇总同步归零。
- 三类读取均使用请求代次；代理和账户额外绑定租户、关键词、渠道、停用筛选、页码和当前代理快照，迟到响应不能覆盖当前范围。
- 代理和账户保存冻结记录、租户、筛选与列表代次；提交前复核目标仍存在且归属一致。
- 编辑既有支付账户时代理和支付渠道只读，提交函数再次拒绝迁移。
- 抽屉打开、保存或读取期间锁定筛选、分页、代理选择、刷新和行操作；保存期间禁止关闭弹窗。
- 新增状态合同 5 项，并更新既有权限合同以验证拆分后的代理/账户错误状态。

## 浏览器验收

- 正常态：代理总数 39、启用代理 39；选定 `并发收款代理-payment-account-permission-1784392467631` 后支付账户 1、启用账户 1。
- 首轮停止 API 后刷新：商家选项和代理列表分别显示 HTTP 502 与独立重试，代理及账户五项汇总全部归零，旧表格数据清空。
- 恢复 API 后分别重试商家选项和代理列表，39/39 恢复。
- 选择有账户代理后再次停止 API，仅切换代理触发账户读取：代理列表继续保留 20 行，支付账户独立清空并显示 HTTP 502 与“重试支付账户”；恢复后重试回到 1/1。
- 打开支付账户编辑弹窗后，关键词、渠道、停用筛选和查询锁定；代理与渠道控件只读。取消弹窗，未保存数据。
- 390x844 视口下页面 `clientWidth=scrollWidth=390`，控制台 warning/error 0。
- 截图：`.local-logs/payment-account-state-20260721-mobile.png`；最终恢复桌面视口和 `/admin/dashboard`。

## 验证

- 专项：`payment-account-permission-contract.spec.ts` 与 `payment-account-state-contract.spec.ts`，2 文件 9 项通过。
- PC 类型检查和生产构建通过。
- 完整 `npm.cmd run ci:verify` 170.4 秒退出 0；运行时 high/critical 0，全部 API 测试、preflight 和 Shared/API/PC/H5/微信小程序构建通过。
- 监控：`status=ok`、`alerts=0`。
- 统一资金：`healthy=true`、`issueCount=0`；活动 330、课程 91、商城 815、钱包流水 463、商城结算 26。
- readiness：`ready=true`、`blockingCount=0`。
- `activity-api`、`activity-mysql` healthy，`activity-nginx` running；差异格式检查退出 0。

## 结论

计划表 `11.01.69` 收款方式状态闭环通过。正式微信/支付宝商户证书、回调、自动打款及真机流程继续归入既有生产外部验收项。
