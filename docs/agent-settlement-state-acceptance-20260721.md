# 代理结算状态闭环验收（2026-07-21）

## 范围

- 页面：`/admin/agent-settlements`
- 账号：`showcase_admin`，租户 `#23 / 慢π演示中心`
- 在既有六权分层、资金并发和隐私治理基础上，补齐请求代次、失败清空、写目标保护、导出筛选和交互锁定。
- 故障演练仅停止并恢复 `activity-api`，未执行生成、审核、拒绝、转账或打款写操作。

## 实现

- 结算选项、结算列表、自动打款能力和详情分别增加请求代次，迟到响应不能覆盖当前筛选或目标。
- 列表和能力评估在读取开始及失败时清空旧数据；列表汇总同步归零。
- 生成结算冻结代理、筛选和列表代次；提交、通过、拒绝、沙箱转账、打款登记复核当前结算单状态、筛选范围和列表代次。
- 回执扫描绑定当前筛选与列表代次；导出补齐租户、代理、关键词和状态全部列表筛选。
- 生成/打款弹窗、详情抽屉、转账评估、导出或写操作期间锁定顶部动作、筛选、分页和行操作；保存期间禁止关闭弹窗。
- 新增状态合同 5 项；保留转账预检要求的 `${row.id}` 路径表达，同时由 `runAction` 完成当前目标与状态复核。

## 浏览器验收

- 正常态：结算单 6、待审核 0、已打款 4、应打款 0.00 元。
- 自动打款能力：启用代理 39、支付账户 6、可沙箱验证 0、真实就绪 0、缺账户代理 33。
- 停止 API 后分别刷新结算列表和能力评估：列表、四项汇总及能力旧数据清空，两个分区分别显示 HTTP 502 和独立重试。
- 恢复 API 后分别重试，结算 `6/0/4/0.00` 和能力 `39/6/0/0/33` 完整回归。
- 打开“生成代理结算”弹窗后，刷新、能力评估、扫描、导出、生成按钮以及代理、关键词、状态、查询全部锁定；取消后未写入数据。
- 390x844 视口页面 `clientWidth=scrollWidth=375`，标题高度 31px，控制台 warning/error 0。
- 截图位于 `.local-logs/agent-settlement-state-20260721/mobile.png`；最终恢复桌面视口和 `/admin/dashboard`。

## 自动化与运行态

- 专项：权限合同与状态合同 2 文件、10 项通过。
- PC 类型检查和生产构建通过。
- 首轮完整 CI 的测试和绝大多数预检通过，代理转账预检要求保留 `${row.id}` 源码表达；等价恢复后专项、预检和完整 `ci:verify` 重跑 164 秒退出 0。
- 运行时依赖 high/critical 0；全部 API 测试、preflight、Shared/API/PC/H5/微信小程序构建通过。
- 监控 `status=ok`、`alerts=0`；统一资金 `healthy=true`、`issueCount=0`；readiness `ready=true`、`blockingCount=0`。
- API/MySQL healthy，Nginx running；差异格式检查退出 0。

## 结论

计划表 `11.01.70` 代理结算状态闭环通过。正式微信/支付宝转账产品、证书、真实回执和真机流程继续作为生产外部验收项。
