# 07.02 课程准入、订单幂等与进度同步验收

## 验收结论

- 状态：已完成；真实微信支付与微信真机在正式配置下复核。
- 订单幂等脚本：`npm run acceptance:course-order-idempotency`。
- 准入模式脚本：`npm run acceptance:course-access-modes`。
- 进度并发脚本：`npm run acceptance:course-progress-concurrency`，已改为无需手工 token 的自包含脚本。

## 订单与并发

- 新会员 `30960 / 13987098434 / Qiwai123456`。
- 免费课程 8 路并发领取只生成订单 `57`，全部请求返回同一已拥有结果。
- 付费课程 8 路并发下单只保留订单 `65 / CO1784257098761F6C`，后台确认线下收款后为 paid。
- 同一 `clientOrderKey` 用于其他课程返回 409。
- 演示租户 token 在杭州租户下创建课程订单返回 404。

## 会员与兑换准入

- 会员专享课程 `16`，会员 `30967 / 13717934595`。
- 等级不足领取返回 403；增加 300 积分/成长后生成零金额 paid 订单 `75`，业务快照记录 `entitlementSource=member`。
- 扣回 300 后会员等级下降，已生成的学习权限继续有效。
- 兑换专享课程 `17`，会员 `30968 / 13727934596`。
- 普通下单返回 400；兑换码 `COURSE4257934595` 8 路并发仅 1 次成功、7 次命中个人上限。
- 同兑换码在杭州租户返回 404；兑换后课程播放器 `owned=true`。

## 学习进度与多端同步

- 课程 `3`、课时 `2` 使用两个不同设备 User-Agent 并发提交 42% 与 83%。
- 已有 100% 进度不被较低并发值覆盖，最终仍为 100%。
- 两设备随后读取均为 100%，最近学习课时为 `2`。
- 非数字进度返回 400。

## 浏览器验收

- 使用兑换会员打开 H5 课程 `17`，页面显示真实课程标题和“继续观看”。
- 页面宽度与视口宽度均为 1179，无横向溢出；console warning/error 为 0。

## 验收中修复

1. 进度并发脚本原先依赖手工 token 和课程参数，改为默认登录演示会员并支持环境变量覆盖。
2. 租户用户归属原先只认可活动报名，课程会员无法由租户 CRM 服务；现同时认可当前租户会员档案。

## 回归结果

- API 全量 103 个测试文件、604 项通过。
- API 生产构建通过。
- 课程并发与准入专项 5 项通过。
- Docker API healthy。
