# 商城订单核心读取状态与故障恢复验收报告

验收日期：2026-07-20  
页面：`/admin/mall-orders?tenantId=23`  
验收账号：`showcase_admin / Qiwai123456`  
验收租户：`23 / 慢π演示中心（qiwai-showcase）`

## 一、整改范围

- 店铺范围、订单列表与汇总、经营看板、售后、支付与佣金、结算单、跨店结算组追踪均增加独立持久错误和重试入口。
- 店铺范围、订单、看板、售后、支付、结算和跨店追踪使用独立请求代次；快速筛选、切换店铺和连续刷新时，旧响应不能覆盖新上下文。
- 所有主读取在请求开始时清除对应旧数据；故障不再把历史订单、金额、流水或支付就绪度留在页面。
- 订单列表与汇总使用 `Promise.allSettled`，分别报告失败分区；支付读取的流水、回调、退款日志、佣金明细、佣金汇总、推广人汇总和支付就绪度七个分区独立恢复。
- 跨店结算组的订单、汇总、售后、支付、回调、退款和佣金八个分区独立恢复，并绑定当前结算组号。
- 对列表、分页对象和汇总对象补响应结构校验，错误响应不能伪装成空业务数据。

## 二、正常态数据

- 筛选订单数 265。
- 实收 10,993.77 元，净收 9,957.02 元，已退 1,036.75 元，待处理售后 21。
- 近 30 天订单 193，实收 10,993.77 元，净收 9,957.02 元。
- 支付日志分区读取 48 条支付流水、1 条回调和 18 条退款日志。
- 桌面页面 `clientWidth=1265 / scrollWidth=1265`，控制台 warning/error 0。

## 三、故障与恢复

受控停止 `activity-api`，MySQL、测试数据和 migration 均未改动：

- 点击主刷新后订单行清零，订单和看板金额全部归零。
- 页面持久显示订单列表、订单汇总及经营看板失败，不再仅弹瞬时消息。
- 支付分区刷新后 48 条流水、1 条回调、18 条退款日志全部清零，支付就绪度清除。
- 支付错误明确列出支付流水、支付回调、退款日志、佣金明细、佣金汇总、推广人汇总和支付就绪度七个失败分区。
- API 恢复 ready 后，订单与看板分别重试回到 265/193 条口径；支付分区独立重试回到 48/1/18 条。

截图：

- `.local-logs/mall-order-state-20260720/desktop-success.png`
- `.local-logs/mall-order-state-20260720/desktop-api-failure.png`

## 四、自动化与运行态

| 检查 | 结果 |
| --- | --- |
| `menu-integrity.spec.ts` | 25/25，通过商城订单主读取状态合同 |
| API 全量测试 | 158 个文件、911 项全部通过 |
| `npm run ci:verify` | 130 秒，退出 0；运行时 high/critical 0，全部 preflight 与四端构建通过 |
| PC 生产构建 | 1947 modules，通过 |
| `npm run monitor:health` | `status=ok`、`alerts=0` |
| 统一资金一致性 | `healthy=true`、`issueCount=0` |
| `/api/health/ready` | `ready=true`、`blockingCount=0` |
| `git diff --check` | 退出 0 |

## 五、后续连续工作

本批完成商城订单工作台的核心读取状态闭环。下一批继续订单详情、跨店子单切换、分包发货、物流设置、优惠券、秒杀、拼团、参团记录和推广码弹窗的店铺/订单上下文绑定、旧数据清理和故障恢复，不将本批误报为整个商城订单页面最终完成。
