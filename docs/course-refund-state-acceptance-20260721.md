# 课程退款状态闭环验收（2026-07-21）

## 范围

- 页面：`/admin/course-refunds`
- 账号：`showcase_admin`，租户 `#23 / 慢π演示中心`
- 在既有课程退款隐私、渠道补偿和权益联动能力上，补齐读取代次、失败清空、写目标保护、交互锁定和移动布局。
- 故障演练仅停止并恢复 `activity-api`；审核确认只打开后取消，未改变退款或课程订单数据。

## 实现

- 退款列表增加请求代次与课程、状态、关键词、分页快照，迟到响应不能覆盖当前筛选。
- 请求开始、响应结构异常或请求失败时清空旧行、总数及四项汇总，避免故障态继续展示历史成功数据。
- 课程 ID 仅接受正整数；非法输入在客户端阻止请求。
- 审核和结果确认冻结退款记录、当前状态、筛选和列表代次，确认后再次复核当前行，避免操作旧目标。
- 从确认框打开起锁定刷新、筛选、查询、分页和全部行操作，取消后恢复。
- 新增总数、待审核、处理中、失败四项汇总，并补齐窄屏筛选布局。

## 浏览器验收

- 正常态：退款总数 15、待审核 1、处理中 0、失败 0。
- 输入课程 ID `abc` 时客户端直接提示且不发起请求。
- 停止 API 后刷新：退款行、总数和四项汇总全部归零，页面显示 HTTP 502 与独立重试入口。
- 恢复 API 后点击重试，真实数据完整恢复为 `15/1/0/0`。
- 打开待审核退款的通过确认框后，刷新、筛选、查询和行操作全部禁用；点击 `Cancel` 取消，业务状态未改变。
- 390x844 视口 `clientWidth=scrollWidth=375`，标题高度 31px，控制台 warning/error 0。
- 截图：`.local-logs/course-refund-state-20260721/mobile.png`。
- 最终恢复桌面工作台，页面宽度 `1081/1081`，控制台 warning/error 0。

## 自动化与运行态

- PC 类型检查与生产构建通过。
- 课程退款专项 3 文件、15 项通过。
- 完整 `npm.cmd run ci:verify` 用时 131.2 秒并退出 0；全部测试、preflight 和 Shared/API/PC/H5/微信小程序构建通过。
- 监控 `status=ok`、`alerts=0`。
- 统一资金 `healthy=true`、`issueCount=0`；活动订单 330、课程订单 91、商城订单 815、钱包流水 463、商城结算 26。
- readiness `ready=true`、`blockingCount=0`。
- `activity-api`、`activity-mysql` healthy，`activity-nginx` running；`git diff --check` 退出 0。

## 结论

计划表 `11.01.71` 课程退款状态闭环通过。正式微信/支付宝退款证书、真实回执及真机流程继续作为生产外部验收项，不阻塞其余本地开发。
