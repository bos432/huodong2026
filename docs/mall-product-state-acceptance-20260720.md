# 商城商品、SKU 与库存弹窗状态验收报告

验收日期：2026-07-20  
页面：`/admin/mall-products`  
验收账号：`showcase_admin / Qiwai123456`  
验收店铺：`38 / 慢π自营店`

## 一、整改范围

- 商家、店铺、商品、分类品牌、低库存和库存异常读取增加独立请求代次、响应结构校验、旧数据清理、持久错误和重试。
- 店铺分类、平台类目和品牌改为 `Promise.allSettled`，单个目录失败不再阻断其他成功分区。
- 主页面目录与商品表单目录完全分离，编辑其他店铺商品不会污染当前列表筛选选项。
- 商品编辑先按商品 ID 读取独立详情，并复核商品、商家和店铺归属；详情成功前不展示列表行快照，保存保持禁用。
- 编辑已有商品时冻结商家和店铺，保存前再次复核打开目标；新建商品使用独立店铺选项，不再改写主页面店铺范围。
- 优惠券、库存流水和审核记录分别绑定打开时店铺、商品和 SKU，增加请求代次、旧数据清理、持久错误与独立重试。
- 库存调整冻结商品、商家、店铺、允许 SKU 集合和业务幂等键，提交前拒绝不属于当前商品的 SKU。
- 商家或店铺范围变化会关闭商品、优惠券、库存流水、审核和库存调整弹窗，并使全部迟到响应失效。
- 390x844 页面头部、店铺操作和筛选栏改为全宽纵向布局，修复标题被挤成单字竖排的问题。

## 二、正常态浏览器验收

- 慢π自营店真实读取 4 个商品：慢π读书手账、东方美学书签套装、节气香囊公益礼盒和书法入门练习套装。
- 商品 `#11 / 【演示】慢π读书手账` 独立详情显示 SPU `P11`，规格为“标准款”和“礼盒款”，库存分别为 120 和 60。
- 同一商品库存流水读取 200 行；审核记录真实为空，没有用列表数据伪造记录。
- 店铺优惠券真实读取 6 条，包括 `EXPIRY07160747`、`BOUNDARY0716055523`、`ONCE5`、`CALLIGRAPHY12`、`STUDY8` 和 `SHOWCASE10`。
- 全程仅打开和取消商品编辑、库存及优惠券弹窗，没有执行保存、库存调整、审核或优惠券写操作。
- 桌面页面 `clientWidth=scrollWidth=1265`；390x844 页面 `clientWidth=scrollWidth=375`，标题宽 327px、高 31px，不再竖排。
- 390x844 商品编辑弹窗宽 390px，文档宽 390px，两个 SKU 均完整加载且无页面级横向溢出。

截图：

- `.local-logs/mall-product-state-20260720/desktop-products.png`
- `.local-logs/mall-product-state-20260720/mobile-products-390x844.png`

## 三、故障与恢复

受控停止 `activity-api` 两次，未停止 MySQL、未执行 migration、未修改商品、SKU、库存或优惠券数据：

- 优惠券刷新后 6 条旧数据清零，显示持久 HTTP 502，新增优惠券按钮禁用；API 恢复后重新打开完整回归 6 条。
- 商品筛选后 4 条旧数据清零，商品和低库存分区分别显示持久 HTTP 502；独立重试后 4 个商品全部恢复。
- 商品 `#11` 详情故障时只保留不可变商品 ID 和店铺归属，商品名、SPU 和 SKU 均为空，显示持久 HTTP 502，保存按钮禁用。
- API 恢复后点击“重新加载商品详情”，SPU、商品名和两条 SKU 完整恢复，保存按钮重新可用。

## 四、自动化与运行态

| 检查 | 结果 |
| --- | --- |
| `menu-integrity.spec.ts` | 26/26，通过商品列表、详情、目录、优惠券、库存和审核弹窗状态合同 |
| API 全量测试 | 158 个文件、912 项全部通过 |
| `npm run ci:verify` | 130.5 秒，退出 0；运行时 high/critical 0，全部 preflight 与全端构建通过 |
| PC 生产构建 | 1947 modules，通过 |
| `npm run monitor:health` | `status=ok`、`alerts=0` |
| 统一资金一致性 | `healthy=true`、`issueCount=0`；活动 330、课程 91、商城 815、钱包流水 463、结算 26 |
| `/api/health/ready` | `ready=true`、`blockingCount=0` |
| `git diff --check` | 退出 0 |

## 五、结论

商城商品主列表、详情表单、SPU/SKU、优惠券、库存流水、低库存、库存异常和审核记录的范围切换、故障恢复及移动布局已闭环。下一工作包继续商城库存、审核或其他 PC 长尾页面的独立状态治理，不提前结束 `11.01` 全页面审计。
