# 商城物流管理状态治理验收报告

## 范围

- 页面：`/admin/mall-logistics?tenantId=23&merchantId=38`
- 账号：`showcase_admin`，运营角色
- 店铺：慢π自营店，店铺 ID `38`
- 验收日期：2026-07-21

## 实现结果

- 商家列表、授权店铺和物流公司列表使用独立请求代次，读取前清空旧数据。
- 物流列表响应绑定租户、店铺、关键词和启停筛选；迟到响应不能覆盖新范围。
- 读取失败保留持久错误和独立重试入口，表格与“总数/启用数”同步归零。
- 编辑表单绑定原物流公司和列表代次；新增、编辑、启停均冻结租户及店铺参数。
- 启停确认后重新从当前列表取得记录并复核目标、店铺、筛选和代次，避免使用旧行快照写入。
- 保存或启停期间，租户、店铺、关键词、状态和刷新入口全部锁定。

## 浏览器验收

| 场景 | 结果 |
|---|---|
| 正常读取 | 顺丰演示、中通演示、京东演示 3 条，启用 3 条 |
| API 中断 | 列表立即清空，总数与启用数为 0；请求结束后显示 HTTP 502 和“重试物流列表” |
| 独立恢复 | API healthy 后点击“重试物流列表”，恢复 3 条且无 502 |
| 写操作上下文 | 打开顺丰停用确认时范围控件锁定；点击取消后顺丰仍启用 |
| 移动布局 | 390x844 视口下文档 `clientWidth=375`、`scrollWidth=375`，标题 323x31px |
| 控制台 | warning 0，error 0 |

截图：`.local-logs/mall-logistics-state-20260721/mobile-390x844.png`。

## 自动化与运行状态

- `menu-integrity.spec.ts`：32/32。
- PC 生产构建：通过。
- `npm run ci:verify`：130.5 秒，退出 0；API 全量测试、全部 preflight、Shared/API/PC/H5/微信小程序构建通过。
- 运行时依赖审计：high 0，critical 0。
- 监控：`status=ok`，`alerts=0`。
- 统一资金：`healthy=true`，`issueCount=0`。
- readiness：`ready=true`，`blockingCount=0`。
- Docker：API、MySQL 均为 healthy。
- 差异格式检查：退出 0，仅存在既有行尾转换提示。

## 外部验收

正式物流查询供应商、真实运单轨迹和微信真机物流展示继续按生产配置清单终验，本工作包未变更真实物流状态或测试数据。
