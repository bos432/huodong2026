# 验收测试报告

验收时间：2026-07-04 至 2026-07-05
分支：`feature/qiwai-ui-experiment`
当前提交：`bb846179 fix mobile activity publish feedback`
线上 API：`https://rd.chaimen666.com/api`
测试商家：`慢π演示中心 / qiwai-showcase`

## 结论

手机端管理端“保存没反应、发布没反应”的核心链路已通过复测。展示商家运营账号可以登录手机端管理端，进入发布活动页，空表单保存会出现明确校验提示，补齐必填信息后点击发布可以创建线上报名中活动。

本轮没有发现还需要继续改代码的新阻塞问题。若最新体验版仍表现为没有反应，最可能原因是微信小程序体验版仍是旧包，或未重新上传/提交审核；服务器部署不会自动刷新微信小程序包。

## 微信开发者工具说明

本机检查到目录 `C:\Users\Administrator\AppData\Local\微信开发者工具`，但只有 `User Data`，没有微信开发者工具可执行程序或 CLI，所以无法在当前环境直接唤起微信开发者工具。

替代验证：

- 已检查 `apps/mobile/dist/build/mp-weixin/pages/admin/activity/edit.wxml`：保存、发布、提交审核按钮均已编译为 `bindtap`。
- 已检查 `apps/mobile/dist/build/mp-weixin/pages/admin/activity/edit.js`：`handleSaveTap`、`handlePublishTap`、`handleSubmitApprovalTap`、`validateBeforeSave`、`mobileAdminRequest` 均存在。
- 已用同一套 uni-app 页面构建出的 H5，在手机尺寸下通过 Chrome 调试接口模拟真实触摸点击，连接线上 API 完成发布。

## 手机端管理端 UI 复测

测试方式：本地 H5 构建 `apps/mobile/dist/build/h5`，手机视口，真实线上 API，触摸事件点击。

测试账号：`showcase_ops / Qiwai123456`

测试结果：

| 步骤 | 结果 |
| --- | --- |
| 登录手机管理端 | 通过，接口 `/admin/auth/login` 返回 201 |
| 首页加载操作区 | 通过，显示发布活动、活动管理、报名审核、签到核销、订单查看 |
| 点击发布活动 | 通过，进入 `/pages/admin/activity/edit` |
| 空表单点击保存 | 通过，页面显示“请填写活动标题” |
| 填写标题、介绍、地点、详情 | 通过 |
| 点击发布 | 通过，接口 `/admin/activities` 返回 201 |
| 发布后读取活动详情 | 通过，接口 `/admin/activities/12` 返回 200 |

保留测试数据：

- 活动 ID：`12`
- 活动标题：`手机端H5点击发布验收 20260705001327`
- 状态：`open / 报名中`
- 证据目录：`.local-logs/mobile-admin-ui-20260705001327/`
- 截图：`01-login.png`、`02-home-ready.png`、`03-edit-empty.png`、`04-validation.png`、`05-filled-details.png`、`06-published.png`

## 线上 API 角色复测

测试批次：`20260704235916`

通过账号：

| 账号 | 密码 | 角色 | 结果 |
| --- | --- | --- | --- |
| `showcase_admin` | `Qiwai123456` | 商家管理员/运营权限 | 登录、bootstrap、活动列表通过 |
| `showcase_ops` | `Qiwai123456` | 商家运营 | 登录、bootstrap、活动列表、创建发布、编辑保存通过 |
| `showcase_finance` | `Qiwai123456` | 财务 | 登录、活动列表通过；创建活动被 403 拒绝，符合权限 |
| `showcase_checkin` | `Qiwai123456` | 核销 | 登录、活动列表通过；创建活动被 403 拒绝，符合权限 |

保留测试数据：

- 活动 ID：`11`
- 初始标题：`手机端管理发布验收 20260704235916`
- 编辑后标题：`手机端管理发布验收 20260704235916 编辑保存`
- 状态：`open / 报名中`
- 群二维码字段：已写入 `groupQrCodeUrl`
- 证据文件：`.local-logs/mobile-admin-20260704235916/api-smoke-report.json`

未通过账号：

| 账号 | 默认密码 | 结果 | 说明 |
| --- | --- | --- | --- |
| `admin` | `Admin123456` | 401 | 线上默认平台超管密码已不可用，需要提供实际密码或重置后补测平台审核 |
| `qiwai_hz_admin` | `Qiwai123456` | 401 | 线上默认杭州账号密码已不可用 |
| `qiwai_hz_ops` | `Qiwai123456` | 401 | 线上默认杭州账号密码已不可用 |
| `qiwai_hz_finance` | `Qiwai123456` | 401 | 线上默认杭州账号密码已不可用 |
| `qiwai_hz_checkin` | `Qiwai123456` | 401 | 线上默认杭州账号密码已不可用 |

这部分不是功能失败，而是账号密码与交付默认值不一致。平台审核流需要拿到真实平台超管账号后补测。

## PC 后台与后端检查

已完成：

- `showcase_*` 多角色登录接口通过。
- 活动列表接口通过。
- 活动创建、编辑、发布接口通过。
- 财务/核销创建活动被权限拦截，符合预期。
- PC 管理后台构建通过。

未完成：

- 未用平台超管账号在 PC 后台做“待审活动通过/驳回”实点复测，因为线上 `admin / Admin123456` 当前返回 401。

## 构建与测试

已执行并通过：

```bash
npm --prefix apps/api run build
npm --prefix apps/api run test
npm --prefix apps/admin run build
npm --prefix apps/mobile run build:h5
npm --prefix apps/mobile run build:mp-weixin
```

结果：

- API 单元测试：14 个测试文件，173 个测试通过。
- API 构建：通过。
- PC 管理后台构建：通过，有 Vite chunk size 体积提示，不影响发布。
- Mobile H5 构建：通过。
- mp-weixin 构建：通过，并已执行小程序授权配置 patch。

## 是否还有类似问题

本轮重点检查了以下同类点：

- 保存/发布按钮编译绑定：通过。
- 空表单无反馈：已改为固定提示和 toast，复测通过。
- 运营/财务/核销权限串用：通过，财务/核销不能创建活动。
- 已上线活动编辑保存：API 复测通过，状态保持 `open`。
- 群二维码字段：活动详情接口可读写 `groupQrCodeUrl`，公开活动页不直接展示二维码，报名详情用于展示。
- H5/小程序 API 地址：mp-weixin 默认兜底到 `https://rd.chaimen666.com/api`，避免小程序包拿不到接口地址。

剩余风险：

- 必须重新上传小程序包并审核。
- 平台超管和杭州商家真实线上密码未提供，平台审核流仍需补测。

