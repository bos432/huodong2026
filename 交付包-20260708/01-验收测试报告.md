# 验收测试报告

## 验收结论
本轮验收通过。新增的运营成熟化、默认城市入口、完整论坛 v1、志愿者证书闭环均已验证；原有活动报名、付款确认、签到、退款、课程、商城、财务、角色权限和手机管理端也完成回归。

## 验收环境
- 本地仓库：`E:\2027\AI全自动开发1.0\活动报名-重编排工作副本`
- 分支：`feature/qiwai-ui-experiment`
- 验收站点：`http://127.0.0.1:18080`
- 验收 API：`http://127.0.0.1:3000/api` 和 `http://127.0.0.1:18080/api`
- 演示商家：`qiwai-showcase`
- 三端版本验收：API、Admin、H5 均读取到 `480f5153`

## 构建验收
已执行并通过：
```bash
npm run build
npm --prefix apps/mobile run build:mp-weixin
```

覆盖：
- `packages/shared` TypeScript 构建
- `apps/api` Nest 构建
- `apps/admin` 后台生产构建
- `apps/mobile` H5 生产构建
- `apps/mobile/dist/build/mp-weixin` 微信小程序构建

构建提示：
- uni-app 提示有新版本，这是工具版本提示，不影响产物。
- H5 构建有第三方库注释和 chunk size warning，不影响功能。

## 接口和业务烟测
已执行并通过：
```bash
$env:SHOWCASE_PASSWORD="Qiwai123456"
$env:SHOWCASE_ADMIN_PASSWORD="Admin123456"
npm run smoke:online-showcase
npm run smoke:operations-forum
```

`smoke:online-showcase` 覆盖：
- H5 首页装修读取。
- 免费报名闭环：报名、我的报名、签到码、后台核销。
- 收费报名余额支付闭环：报名、订单、余额扣款、钱包流水。
- 退款闭环：用户申请、财务退款、审核通过、余额退回。
- 动态互动闭环：点赞、评论待审、后台通过、前台可见。
- 课程交付闭环：下单、后台确认、播放权限、学习进度、我的课程。
- 商城闭环：榜单、搜索、优惠券、低库存、收藏足迹、购物车、拼团、订单取消、幂等下单、超时关闭、物流、评价、售后、财务对账和导出。

`smoke:operations-forum` 覆盖：
- 默认城市入口：`bootstrap.defaultTenant.code = qiwai-showcase`。
- 六个运营总览：公益、大使、志愿者、课程、共修、论坛 overview 均返回 KPI 和待办。
- 完整论坛 v1：版块、发帖待审、审核、置顶、精华、收藏、回复、楼中楼、举报处理。
- 志愿者证书闭环：任务、报名、审核、服务记录、时长累计、发证、我的证书、公开核验、撤销。

专项验收保留用户：
- `13991021109 / Qiwai123456`

## 浏览器验收
已执行并通过：
```bash
$env:SHOWCASE_PASSWORD="Qiwai123456"
$env:SHOWCASE_ADMIN_PASSWORD="Admin123456"
npm run browser:online-showcase
npm run browser:mobile-admin
npm run browser:operations-forum
```

### H5 和后台主流程
结果文件：
`E:\2027\AI全自动开发1.0\活动报名-重编排工作副本\.local-logs\browser-acceptance-20260708021409\result.json`

主要通过项：
- H5 新手机号验证码登录。
- H5 付费活动线下收款报名。
- 财务角色确认线下收款。
- H5 报名详情付款后生成签到码。
- 签到角色手工核销签到码。
- 签到页 375、768、1280 视口回归。
- H5 报名详情签到状态刷新。
- 平台超管、商家管理员、运营、财务、签到、店铺负责人、店铺财务、代理负责人角色权限。
- 营销弹窗、广告位图片、会员详情时间线、首页模板、系统设置版本和短信卡片、H5 首页弹窗广告首屏。

保留数据：
- 测试手机号：`13976849790`
- 报名 ID：`208`
- 订单号：`OD1783476852570208`
- 签到码：`a689ce59-559a-4add-84a7-3b7e62e0d0f8`

### 手机管理端
结果文件：
`E:\2027\AI全自动开发1.0\活动报名-重编排工作副本\.local-logs\mobile-admin-acceptance-20260708021525\result.json`

主要通过项：
- 手机管理端登录和工作台。
- 活动列表。
- 新建活动并保存草稿。
- 发布活动。
- 报名列表。
- 订单列表。
- 签到核销页。

保留数据：
- 活动标题：`【手机验收保留】活动发布 20260708021525`
- 活动 ID：`136`

### 默认城市和论坛专项浏览器验收
结果文件：
`E:\2027\AI全自动开发1.0\活动报名-重编排工作副本\.local-logs\browser-operations-forum-20260708021545\result.json`

主要通过项：
- H5 默认入口进入论坛。
- H5 论坛详情。
- H5 论坛发帖页。
- 后台共修动态论坛管理页签。
- 后台默认入口城市配置。

保留数据：
- 论坛帖子 ID：`9`

## 发布前审计
已执行并通过：
```bash
npm run codex:audit
npm run codex:release-check
```

`codex:release-check` 通过项：
- doctor
- preflight guards
- release preflight
- API unit tests：16 个测试文件、181 个测试通过
- full web build
- wechat mini program build
- browser online showcase acceptance
- browser mobile admin acceptance

审计报告路径：
`E:\2027\AI全自动开发1.0\活动报名-重编排工作副本\docs\auto-audit-report-latest.md`

说明：
- 审计状态显示 WARNING 的原因是当时仍有本轮待提交改动，这是交付前正常状态。
- 生产短信、邮件、微信消息服务属于线上凭据配置项，不是代码阻断项。

## 截图证据目录
- 主浏览器验收截图：`E:\2027\AI全自动开发1.0\活动报名-重编排工作副本\.local-logs\browser-acceptance-20260708021409`
- 手机管理端截图：`E:\2027\AI全自动开发1.0\活动报名-重编排工作副本\.local-logs\mobile-admin-acceptance-20260708021525`
- 默认城市和论坛专项截图：`E:\2027\AI全自动开发1.0\活动报名-重编排工作副本\.local-logs\browser-operations-forum-20260708021545`

## 小程序验收说明
小程序产物已编译通过，目录为：
`E:\2027\AI全自动开发1.0\活动报名-重编排工作副本\apps\mobile\dist\build\mp-weixin`

小程序不能只靠服务器部署更新，必须用微信开发者工具导入该目录后上传体验版或提交审核。小程序默认 API 域名为：
`https://rd.chaimen666.com/api`
