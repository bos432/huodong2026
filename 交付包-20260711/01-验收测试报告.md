# 验收测试报告

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 验收日期 | 2026-07-11 |
| 分支 | `feature/qiwai-ui-experiment` |
| 基线提交 | `7fead33b`，本轮改动尚在工作区 |
| 本地入口 | `http://127.0.0.1:18080` |
| API | `http://127.0.0.1:18080/api` |
| 数据库 | Docker MySQL 8.4，保留验收数据 |
| 浏览器视口 | 移动端 390x844，并在结束后恢复默认视口 |

## 2. 测试方法

- 单元测试和实体元数据回归测试。
- 预检守卫，对安全、租户、权限、支付、短信、备份、迁移、Nginx、发布和回滚配置做静态检查。
- API、后台、H5、小程序全量构建。
- 自动化浏览器全流程验收。
- 右侧浏览器人工复核关键状态和控制台日志。
- Docker 运行环境健康检查和运行时依赖审计。

## 3. 自动化结果

| 测试项 | 结果 | 说明 |
| --- | --- | --- |
| `npm run ci:verify` | 通过 | 审计、测试、守卫、全量构建、小程序构建全通过 |
| API 单元测试 | 通过 | 17 个测试文件，182 个用例 |
| 预检守卫 | 通过 | 全部 guard 返回 OK |
| API 构建 | 通过 | NestJS 11 生产构建成功 |
| Admin 构建 | 通过 | Vue/Vite 构建成功 |
| H5 构建 | 通过 | uni-app H5 构建成功 |
| 微信小程序构建 | 通过 | 自动切换兼容 Node 运行时后构建成功 |
| `npm run codex:release-check` | 通过 | 8 个发布阶段全部通过 |
| 运行时依赖审计 | 通过门禁 | 3 中危、0 高危、0 严重 |

发布巡检仅有“工作区存在未提交改动”警告，这是本轮交付改动尚未提交造成，不属于产品缺陷。

## 4. 多角色验收

| 角色 | 账号 | 验收内容 | 结果 |
| --- | --- | --- | --- |
| H5 普通用户 | `13990000002` | 验证码登录、报名、余额支付、查看订单、签到状态 | 通过 |
| 平台超级管理员 | `admin` | 平台配置、租户和全局管理入口 | 通过 |
| 演示商家管理员 | `showcase_admin` | 工作台、活动、报名、订单、会员、系统设置 | 通过 |
| 演示运营 | `showcase_ops` | 活动运营和移动管理后台 | 通过 |
| 财务 | `showcase_finance` | 收款、退款、财务记录和权限隔离 | 通过 |
| 签到员 | `showcase_checkin` | 签到码核销和受限菜单 | 通过 |
| 店铺负责人 | `showcase_store_owner` | 店铺商品、订单和履约 | 通过 |
| 店铺财务 | `showcase_store_finance` | 店铺订单、结算，不能编辑商品 | 通过 |
| 代理负责人 | `showcase_agent_owner` | 代理业务和结算 | 通过 |

## 5. 用户全流程保留数据

1. 用户 `13990000002` 使用开发验证码 `123456` 登录。
2. 报名活动 ID `105`，生成报名 ID `219`。
3. 生成订单 `OD1783729160570219`，金额 `199.00` 元。
4. 使用余额支付，订单状态变为“已付款”。
5. 签到员使用签到码 `e544036e-47a7-4de8-a56f-1c0c214066c3` 完成核销。
6. 用户报名详情最终显示“已付款”和“已签到”。
7. 测试备注为 `交付全流程验收 20260711`。

## 6. 业务模块结果

| 模块 | 验收范围 | 结果 |
| --- | --- | --- |
| 活动 | 创建、发布、列表、报名、审核、订单、签到 | 通过 |
| 支付 | 余额支付、线下收款状态、退款闭环 | 通过；真实支付外部配置见限制清单 |
| 社区 | 共修活动、动态发布与可见性 | 通过 |
| 课程 | 课程浏览、订单与学习入口 | 通过 |
| 商城 | 优惠券、拼团、物流、售后、财务、结算 | 通过 |
| 多商户 | 店铺与代理角色隔离、订单归属 | 通过 |
| 移动管理端 | 首页统计、活动、报名、订单、签到 | 通过 |
| SaaS 权限 | 平台、租户、店铺、代理数据边界 | 通过 |

## 7. 右侧浏览器最终复核

### 移动管理后台

- 活动总数：24。
- 待审活动：0。
- 报名累计：45。
- 本月报名：42。
- 可见最近保留的手机验收活动。
- 浏览器控制台：0 条 warning，0 条 error。
- 证据：`验收截图/01-移动端管理后台-首页.png`。

### H5 用户报名详情

- 报名状态：已签到。
- 订单状态：已付款。
- 实付金额：199.00 元。
- 用户姓名：交付验收用户B。
- 手机号：13990000002。
- 浏览器控制台：0 条 warning，0 条 error。
- 证据：`验收截图/02-H5用户端-报名已付款已签到.png`。

## 8. 验收结论

本地交付环境核心功能开发完成，已达到可交付和可部署状态。没有发现需要继续修改的代码级阻塞问题。本轮发现的移动管理后台统计字段问题已修复并完成回归。

真实短信、真实微信/支付宝支付和微信小程序真机验收依赖甲方账号、证书、模板和平台权限，不能用本地假配置代替；这些事项不影响源码和本地业务闭环验收，但属于正式运营前置条件。

